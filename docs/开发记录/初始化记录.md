# 项目初始化记录

**执行时间**: 2026-07-05
**执行人**: AI Assistant (Role: DevOps / Scaffolder)

## 1. 检查项
- [x] 技术栈文档（飞书《技术栈》）
- [x] 项目结构文档（飞书《项目结构》）
- [x] 开发规范文档（飞书《开发规范》）
- [x] 目标目录检查（`G:\副业\仿校园小程序开发\project`，骨架已存在，安全）
- [x] 环境检查：node v22.12.0 / pnpm 10.6.5 / git 2.36.1

> 说明：规划文档均在飞书「家教平台小程序」Wiki 节点下，非本地 `specs/`。

## 2. 执行日志

### 2.1 核心初始化
- 骨架（目录 + 根文件 + `git init`）已在 project-structure 阶段完成；本次做**框架级脚手架**。
- 依赖安装：`pnpm install`（470 包，34.6s）成功。
- Prisma Client 生成：`prisma generate` 成功（修正 enum 多行格式后）。

### 2.2 目录与工程创建
- [x] 根开发配置：`.prettierrc` / `.editorconfig` / `eslint.config.mjs` / `tsconfig.base.json`
- [x] `packages/shared-types`（package.json + tsconfig + src/index.ts，已 build）
- [x] `packages/shared-utils`（同上，已 build）
- [x] `apps/server`（NestJS：package.json / tsconfig / tsconfig.build / nest-cli.json / main.ts / app.module.ts / PrismaModule+Service / AllExceptionsFilter / api-response；`prisma/schema.prisma`）
- [x] `apps/user-miniprogram`（原生小程序：app.ts/json/wxss / sitemap / project.config / services/request.ts / 4 个 tabBar 页面）
- [ ] `apps/admin-web`（Vue3）— 留待路线图 W4 阶段 scaffold
- [ ] `apps/merchant-miniprogram`（商家端）— 留待路线图 W2 阶段 scaffold

### 2.3 配置文件
- [x] `.gitignore`（覆盖 node_modules / dist / .env / 小程序私有配置）
- [x] `README.md`（含启动说明）
- [x] `.env.example`
- [x] `apps/server/.env`（本地开发占位，已 gitignore）
- [x] `pnpm-workspace.yaml`
- [x] 根 `package.json` 增加 lint / format / typecheck / build:packages 脚本与 `pnpm.onlyBuiltDependencies`

### 2.4 验证
- [x] `apps/server` typecheck（`tsc --noEmit`）通过
- [x] 共享包 build 通过
- [x] Prisma Client 生成通过

## 3. 遗留问题 / TODO
- [ ] 用户需在 `apps/server/.env` 填入真实 `DATABASE_URL` 后执行 `pnpm --filter @yitong/server prisma:migrate` 建表
- [ ] 小程序：在微信开发者工具中替换 `project.config.json` 的 appid 为实际 AppID；如需 TDesign，`npm i tdesign-miniprogram` 后用工具「构建 npm」
- [ ] `apps/admin-web` / `apps/merchant-miniprogram` 待各自阶段 scaffold
- [ ] pnpm 首次安装时 prisma/nestjs 的 postinstall 被拦（已加 `onlyBuiltDependencies`，下次 install 生效）；当前 `prisma generate` 已手动执行

## 4. 下一步建议
按飞书《开发路线图》开始 Milestone 1 / Phase 1.1：开发 `auth` 模块（微信登录 + JWT）与平台底座，并执行首次 Prisma migration。
