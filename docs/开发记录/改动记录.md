# 代码改动记录

> 记录每次代码改动：发生时间、新增/修改内容、完成的工作、潜在隐患、后续待做工作/方向。新条目置顶。
> 维护方式：每次开发完成后在本文件顶部追加一条；已解决的隐患标 ✅。

---

## 2026-07-20 · P0-01~07 全局 UI 统一 + 首页重构 + 推荐流

**发生时间**：2026-07-20 20:00 +0800
**状态**：已审查（双端 typecheck 通过）
**commit**：f5c087d / 419a32f / 2407ff5 / 91abda1 / d3569d9

### 改动概要
按 docs/任务规划.md P0 顺序完成 7 项：全局 UI 样式抽取 + tabBar 统一（品牌黄 #F9C801，去旧粉色）+ 表白墙/树洞/兼职/我的 4 个首页重构 + 推荐/最新/热门流。

### 完成的工作
- ✅ P0-01 全局 UI：app.wxss 定义品牌黄/中性色/状态色变量 + 通用卡片/按钮/标签/空状态类
- ✅ P0-02 tabBar：图标重新生成（选中态黄圆+深图，普通态灰圆+白图）+ selectedColor #1D2129
- ✅ P0-03 表白墙首页：搜索+发布入口、公告运营位、推荐/最新/热门/关注 tab、统一帖子卡片
- ✅ P0-04 树洞首页：匿名身份卡、心情/匹配/群聊入口、推荐/最新/情绪 tab
- ✅ P0-05 兼职首页：搜索+商家入口、推荐/最新/急招 tab、统一岗位卡片（薪资深黄）
- ✅ P0-06 我的页：身份卡（黄渐变）+ 我的内容/账号分组菜单 + 简历入口占位
- ✅ P0-07 推荐/最新/热门流：FeedQueryDto 加 sort，hot 按 likeCount+comments._count，recommend 按热度+新鲜度
- ✅ 任务规划.md P0-01~07 标已完成

### 潜在隐患
1. 关注流 tab（follow）占位 toast，P0-08 实现。
2. 急招/情绪 tab 数据未区分（都走通用流），P0-13/P0-17 区分。
3. 搜索入口占位 toast，P1-05/06/07 实现；简历入口占位，P1-20 实现。
4. hot/recommend 不分页（首页 take 20，hasMore=false）。
5. post-card 组件样式用全局变量（CSS 变量穿透组件），需实机验证渲染。

### 后续待做工作 / 方向
1. P0-08 关注作者与关注流（需 Follow 表 schema 改 + Docker migration）。
2. P0-09~19 其余 P0 任务（大多需 schema 改）。
3. push 到 GitHub（之前 10054，用户手动）。

---

## 2026-07-20 - docs/task-plan 阶段任务规划落地

**发生时间**：2026-07-20 00:00 +0800
**状态**：待检查
**commit**：`36ec379`

### 改动概要
根据 `docs/功能规划.md` 和当前代码基线，新增阶段任务规划文档，把表白墙、树洞、兼职、UI、后台、运维等工作拆入 P0/P1/P2/P3，并为每个任务标注「已完成」或「待实现」；同步更新 `AGENTS.md`，要求后续开发必须维护任务状态并在改动记录写明任务 ID。

### 新增文件
- `docs/任务规划.md` - 分阶段任务表、当前基线判断、状态规则、不做项和维护要求

### 修改文件
- `AGENTS.md` - 增加后续开发必须同步维护 `docs/任务规划.md` 的约束
- `docs/开发记录/改动记录.md` - 追加本次文档改动记录

### 完成的工作
- 建立 P0/P1/P2/P3 阶段任务表。
- 每个任务都设置状态，完成项标「已完成」，未完成或仅有基础能力的项标「待实现」。
- 补充每个任务的验收口径和备注，避免后续只按标题实现。
- 明确后续完成任务时必须更新任务规划状态，并在改动记录写任务 ID。

### 潜在隐患
1. **部分现有能力只算基础实现**：例如表白墙列表、树洞匹配、兼职报名已有基础，但未达到完整规划的任务仍标为「待实现」。
2. **任务数量较多**：后续需要按 P0 优先，不建议同时铺开过多模块。
3. **当前 main 已有未推送提交**：本次文档提交会叠加在本地 main 的未推送提交链之后。

### 后续待做工作 / 方向
1. 按 `docs/任务规划.md` 从 P0 开始做差距修复和 UI 重构。
2. 每完成一个任务同步更新任务状态和改动记录。
3. 阶段完成后用空上下文子代理按任务 ID 复核完成情况。

---

## 2026-07-20 - docs/ui-guideline 统一 UI 设计规范

**发生时间**：2026-07-20 00:00 +0800
**状态**：待检查
**commit**：`883d18a`

### 改动概要
根据用户确认的“统一平台，而不是三个频道三套风格”方向，新增 `docs/UI设计规范.md`，确定全平台统一品牌主色 `#F9C801`，规定三端统一视觉、组件、页面结构、状态色和首批 UI 重构目标；同步更新 `AGENTS.md`，要求后续前端实现必须遵守该设计基线。

### 新增文件
- `docs/UI设计规范.md` - 统一平台视觉、色彩、组件、三端规则和首批重构目标

### 修改文件
- `AGENTS.md` - 增加后续 UI 实现必须对齐 `docs/UI设计规范.md` 的约束
- `docs/开发记录/改动记录.md` - 追加本次文档改动记录

### 完成的工作
- 确定品牌主色为 `#F9C801`，并补充深黄、浅黄底、中性色和状态色。
- 明确表白墙、树洞、兼职不做独立频道配色，只用统一平台色彩。
- 规范按钮、卡片、标签、输入、tab、空状态和图标的基础样式。
- 规定用户端、商家端、管理端在同一视觉系统下的差异化信息密度。
- 明确首批 UI 重构优先级：全局变量、tabBar、四个首页、详情页、匹配页、聊天页。

### 潜在隐患
1. **亮黄色主色需要克制使用**：不能大面积铺底，也不能黄底白字小文本，否则会显廉价或可读性差。
2. **现有页面需要系统性重构**：只替换颜色无法解决 Demo 感，需要统一卡片、间距、按钮和页面结构。
3. **管理端尚未按规范重构**：后续需要单独处理高密度表格和审核队列体验。

### 后续待做工作 / 方向
1. 抽取小程序全局设计变量和基础组件样式。
2. 先重构表白墙首页、树洞首页、兼职首页、我的页，建立平台统一观感。
3. 再重构详情页、发布页、匹配页、聊天页和商家端工作台。

---

## 2026-07-20 - docs/product-scope 完整功能规划落地

**发生时间**：2026-07-20 00:00 +0800
**状态**：待检查
**commit**：`bdc8b57`

### 改动概要
根据用户重新确认的产品范围，将表白墙、树洞、兼职三大模块整理成可落地的功能规划文档，明确树洞保留语音消息但不做视频、实时语音房、语音通话、礼物/打赏和所有 AI 能力；同步更新 `AGENTS.md`，要求后续开发以该功能规划为产品基线。

### 新增文件
- `docs/功能规划.md` - 三大模块功能清单、落地说明、分期建议和验收原则

### 修改文件
- `AGENTS.md` - 增加后续实现必须对齐 `docs/功能规划.md` 的约束
- `docs/开发记录/改动记录.md` - 追加本次文档改动记录

### 完成的工作
- 表白墙模块按内容流、发布、互动、搜索、我的表白墙、消息通知、安全运营拆分。
- 树洞模块按匿名身份、灵魂测试、广场、匹配、1v1 聊天、群聊广场、明确不做项拆分。
- 兼职模块按找兼职首页、岗位分类、筛选搜索、详情、报名、简历、沟通通知、评价保障、商家招聘闭环拆分。
- 明确所有 AI 相关能力不做。
- 明确树洞语音只做异步语音消息，不做实时语音匹配、语音房、语音通话、多人连麦。
- 明确后续开发不得继续按旧 MVP 范围作为目标。

### 潜在隐患
1. **范围明显扩大**：当前代码只覆盖少量基础能力，后续需要重新拆任务和排期。
2. **部分能力依赖后台管理**：举报、禁言、封禁、置顶、加精、下架、工单、账单等需要管理端配套。
3. **合规风险需单独确认**：性别要求、电话联系保护、匿名追溯、内容审核、薪资保障等上线前需要确认规则。

### 后续待做工作 / 方向
1. 按 `docs/功能规划.md` 重新拆 P0/P1/P2 开发任务。
2. 对现有代码做差距分析，标出已实现、部分实现、未实现。
3. 先从 P0 主链路开始补齐，避免继续堆零散增强项。

---

## 2026-07-18 · fix(mp): 修复启动直进表白墙，应先进角色选择页

**发生时间**：2026-07-18 17:00 +0800
**状态**：已审查（小程序 tsc --noEmit 通过；开发者工具实机待验证）
**commit**：`d3e981c`

### 改动概要
修复启动入口：`app.json` 的 `pages` 数组第一项是微信小程序的启动页（硬规则），原第一项为 `confession/index` 导致打开即进表白墙，未先选角色。把 `role-select/index` 提到 `pages[0]`；`app.ts` 的 `onLaunch` 在 `restoreAuth` 失败（无 token）时 `wx.reLaunch` 到角色选择页兜底。

### 修改文件
- `apps/user-miniprogram/app.json` - `pages[0]` 改为 `pages/role-select/index`（原在第 7 位）
- `apps/user-miniprogram/app.ts` - `onLaunch` 无 token 时 `wx.reLaunch` 角色选择页

### 完成的工作
- ✅ 启动页 = 角色选择页（`pages[0]`）
- ✅ `onLaunch` 兜底：无 token 强制 reLaunch 角色选择（`restoreAuth` 同步读 storage，已登录用户不误跳）
- ✅ 小程序 `tsc --noEmit` 通过

### 潜在隐患
1. 已登录用户 `restoreAuth` 成功 -> 不跳转，进 tabBar 首页（表白墙），符合预期。
2. `tabBar` 首项仍是 `confession`（非 `role-select`），但 `role-select` 不在 tabBar 里，启动页与 tabBar 首项分离是正确的（角色选择页不是 tab）。

### 后续待做工作 / 方向
1. 开发者工具实机验证：未登录启动 -> 角色选择；已登录启动 -> 表白墙。

---

## 2026-07-18 - 修复小程序启动报错（根配置缺 miniprogramRoot + WXML 非法表达式）

**发生时间**：2026-07-18 00:00 +0800
**状态**：已审查（小程序 tsc --noEmit 通过；开发者工具实机编译待人工验证）
**commit**：`d3e981c`（随启动页修复一起提交）

### 改动概要
修复两个小程序启动/编译报错：1）微信开发者工具以仓库根目录打开项目时报「在根目录没有找到 app.json 文件」：根目录 `project.config.json` 未声明 `miniprogramRoot`，而小程序源码实际位于 `apps/user-miniprogram/`，在根配置中补充 `miniprogramRoot: "apps/user-miniprogram/"`。2）WXML 编译错误：`{{'★'.repeat(item.rating)}}` 使用了 WXML 表达式不支持的字符串方法，改为在页面 JS 侧预计算 `stars` 字段后渲染（job/detail 与 merchant/reviews 两处）。

### 新增文件
- 无

### 修改文件
- `project.config.json` - 新增 `miniprogramRoot: "apps/user-miniprogram/"`
- `apps/user-miniprogram/pages/job/detail/index.ts` - reviews 预计算 `stars` 字段
- `apps/user-miniprogram/pages/job/detail/index.wxml` - 星级渲染改用 `item.stars`
- `apps/user-miniprogram/pages/merchant/reviews/index.ts` - reviews 预计算 `stars` 字段
- `apps/user-miniprogram/pages/merchant/reviews/index.wxml` - 星级渲染改用 `item.stars`

### 完成的工作
- 根目录打开项目时开发者工具能正确定位 `apps/user-miniprogram/app.json`。
- 清除两处 WXML 非法表达式（`'★'.repeat(...)`），全局排查确认无其他同类用法。
- 小程序 `tsc --noEmit` 通过。

### 潜在隐患
1. `apps/user-miniprogram/project.config.json` 与根配置并存，两套配置的 appid / setting 需保持一致，后续改配置注意同步。
2. 若后端评价接口 rating 字段非整数或越界（>5），星级展示需另行钳制。

### 后续待做工作 / 方向
1. 微信开发者工具重新打开根目录验证编译启动正常、评价星级展示正确。

## 2026-07-16 - feat/wechat-subscribe-message 微信订阅消息接入

**发生时间**：2026-07-16 00:00 +0800
**状态**：已审查（server typecheck + server build + Prisma validate + 小程序 typecheck + 空上下文子代理复核通过；微信订阅消息仍需后台模板与真机联调）
**commit**：`2e6d84a`（squash 合并 feat/wechat-subscribe-message）

### 改动概要
按“接入微信订阅消息，但不做 IM 离线推送”的范围推进：后端新增微信订阅消息发送服务，复用现有平台级 access_token；站内通知创建后异步尝试发送对应订阅消息；新增订阅模板 ID 查询接口供小程序申请授权；小程序在学生报名和商家订阅报名提醒两个用户点击场景里触发 `wx.requestSubscribeMessage`；新增 `.env.example` 记录微信订阅消息模板 ID 配置项。

### 新增文件
- `.env.example` - 补充本地/生产环境变量示例，含微信订阅消息模板 ID
- `apps/server/src/common/wx/wx-subscribe-message.service.ts` - 微信订阅消息模板配置与发送服务
- `apps/user-miniprogram/services/subscribe-message.ts` - 小程序订阅消息授权工具

### 修改文件
- `apps/server/src/common/wx/wx.module.ts` - 导出订阅消息服务
- `apps/server/src/modules/notification/notification.controller.ts` - 新增订阅模板 ID 查询接口
- `apps/server/src/modules/notification/notification.service.ts` - 站内通知创建后异步尝试发送订阅消息
- `apps/user-miniprogram/pages/job/detail/index.ts` - 报名前申请报名状态订阅，商家可订阅报名提醒
- `apps/user-miniprogram/pages/job/detail/index.wxml` - 增加商家订阅报名提醒按钮
- `apps/user-miniprogram/pages/job/detail/index.wxss` - 增加订阅按钮样式

### 完成的工作
- 不接第三方 IM 离线推送，只接微信订阅消息用于低频关键通知。
- 后端支持 `job_apply`、`job_accept`、`job_complete` 三类通知映射到微信订阅消息模板。
- 订阅消息发送失败只记录日志，不影响报名、录用、完成等主流程。
- 缺少微信凭证或模板 ID 时自动跳过发送，避免开发环境被阻断。
- 小程序从后端读取模板 ID，不在前端硬编码模板配置。

### 潜在隐患
1. **模板字段需与微信后台一致**：当前服务端按 `thing1/thing2/time3` 和 `thing1/phrase2/thing3/time4` 组装，需要在微信公众平台创建匹配字段的模板。
2. **订阅消息必须由用户主动授权**：学生只有点击报名时会申请报名状态订阅，商家需要在岗位详情点击“订阅报名提醒”。
3. **真实发送需配置微信凭证与模板 ID**：未配置 `WX_USER_APPID`、`WX_USER_SECRET` 或模板 ID 时只跳过发送。
4. **仍需微信开发者工具验收**：授权弹窗、模板 ID 返回、消息发送结果需要真机/开发者工具联调确认。

### 后续待做工作 / 方向
1. 在微信公众平台创建并确认两个订阅消息模板字段。
2. 将 `WX_SUBSCRIBE_JOB_APPLY_TEMPLATE_ID`、`WX_SUBSCRIBE_JOB_STATUS_TEMPLATE_ID` 填入真实服务端 `.env`。
3. 微信开发者工具/真机验证报名、录用、完成三条链路的订阅授权和触达。

---

## 2026-07-14 - feat/ops-im-refund 基础设施与体验补强

**发生时间**：2026-07-14 23:51 +0800
**状态**：已审查（server typecheck + server build + Prisma validate + 小程序 typecheck + 空上下文子代理复核通过；匿名脚本 dry-run 因本地 Postgres 未启动未完成）
**commit**：`e77f220`（squash 合并 feat/ops-im-refund）

### 改动概要
按后续待补事项顺序推进：新增旧匿名 ID 批量旋转脚本（默认 dry-run，`--apply` 才写库）；增强小程序 IM 连接管理（登录握手、心跳、指数退避重连、离线发送队列、重连后 flush）；服务端聊天补历史分页钳制和派对房房间名限制；补支付退款基础流程（订单记录退款时间/原因，开发环境可标记退款并下架岗位，生产环境真实微信退款未接入时拒绝假退款）；补消息中心 target 跳转、profile 未读角标、收藏状态 check 接口与详情页预填。

### 新增文件
- `apps/server/prisma/scripts/rotate-legacy-anon-ids.ts` - 旧匿名 ID 批量旋转脚本
- `apps/server/prisma/migrations/20260714233000_payment_refund_fields/migration.sql` - 支付订单退款字段迁移

### 修改文件
- `apps/server/package.json` - 新增 `anon:rotate-legacy` 脚本
- `apps/server/prisma/schema.prisma` - PaymentOrder 增加 `refundedAt` / `refundReason`
- `apps/server/src/modules/chat/chat.controller.ts` - 消息历史 limit 钳制
- `apps/server/src/modules/chat/chat.gateway.ts` - 限制派对房房间名，透出 join_failed
- `apps/server/src/modules/chat/chat.service.ts` - HTTP 聊天落库路径复用匿名匹配校验
- `apps/server/src/modules/favorite/favorite.controller.ts` - 新增收藏状态 check 接口
- `apps/server/src/modules/favorite/favorite.service.ts` - 新增收藏状态查询
- `apps/server/src/modules/merchant/merchant.service.ts` - 商家订单返回退款时间和原因
- `apps/server/src/modules/payment/dto/payment.dto.ts` - 新增退款 DTO
- `apps/server/src/modules/payment/payment.controller.ts` - 新增退款接口
- `apps/server/src/modules/payment/payment.service.ts` - 新增退款状态流
- `apps/server/src/modules/treehole/treehole.controller.ts` - 新增匿名聊天消息发送/历史接口
- `apps/server/src/modules/treehole/treehole.service.ts` - 匿名聊天复用 ChatService 落库和匹配校验
- `apps/user-miniprogram/services/im.ts` - 重写 IM 连接管理器
- `apps/user-miniprogram/services/payment.ts` - 新增退款调用
- `apps/user-miniprogram/services/treehole.ts` - 新增匿名消息发送/历史接口调用
- `apps/user-miniprogram/services/merchant.ts` - 商家订单类型补退款字段
- `apps/user-miniprogram/services/favorite.ts` - 新增收藏状态 check
- `apps/user-miniprogram/pages/merchant/orders/index.ts` - 商家订单页增加退款操作
- `apps/user-miniprogram/pages/merchant/orders/index.wxml` - 展示退款原因与退款按钮
- `apps/user-miniprogram/pages/merchant/orders/index.wxss` - 退款状态/按钮样式
- `apps/user-miniprogram/pages/notifications/index.ts` - 消息点击跳转目标详情
- `apps/user-miniprogram/pages/profile/index.ts` - 拉取未读消息数
- `apps/user-miniprogram/pages/profile/index.wxml` - 展示未读角标
- `apps/user-miniprogram/pages/profile/index.wxss` - 未读角标样式
- `apps/user-miniprogram/pages/post-detail/index.ts` - 表白墙详情预填收藏状态
- `apps/user-miniprogram/pages/job/detail/index.ts` - 岗位详情预填收藏状态
- `apps/user-miniprogram/pages/treehole/chat/index.ts` - 匿名 1v1 发送先 HTTP 落库再 WS 实时通知

### 完成的工作
- 旧匿名 ID 批量脚本默认只打印待旋转记录，降低误操作风险。
- `--apply` 模式会级联更新 AnonymousPost、AnonPostLike、ChatMatch、ChatMessage、ChatSession 和 AnonymousProfile。
- IM 连接以 `login_ok` 为成功点；断线后指数退避重连，离线期间消息/入房帧进入有限队列。
- 派对房只允许当前 `treehole-party-main`，非法房间返回 `join_failed`。
- HTTP 聊天发送也会校验匿名双方是否存在 ACTIVE 匹配，避免绕过 WebSocket 网关鉴权直接落库。
- 树洞匿名 1v1 聊天新增 anonToken 鉴权的 HTTP 落库路径，历史消息可通过匿名接口拉取。
- 匿名随机匹配入队前清理同 anonId 等待项，匹配成功后清理双方残留队列项，避免重复入队导致多重 ACTIVE 匹配。
- `/treehole/match` 发现当前 anonId 已有 ACTIVE 匹配时直接返回原匹配和 IM 凭证，避免已匹配用户再次生成第二条 ACTIVE。
- 商家可对自己的已支付订单发起退款；开发环境会标记 REFUNDED 并下架对应已发布岗位。
- 生产环境退款未接入微信真实退款时直接抛 `90003`，不做假退款。
- 消息中心点击通知会按目标类型跳转帖子、树洞、岗位或报名列表。
- 个人中心显示未读消息角标。
- 帖子/岗位详情进入时会预先查询收藏状态。

### 潜在隐患
1. **真实微信退款仍未接入**：本轮只补状态流和防假退款，生产环境仍需接微信支付 V3 refund API 后才能真正退款。
2. **IM 仍是自建 WebSocket 基础版**：已补重连和队列，但未接 MobileIMSDK、小程序 SDK QoS、离线推送。
3. **匿名批量脚本需要在可控窗口执行**：脚本会写多张表，正式执行前应先 dry-run 并备份数据库。
4. **体验增强仍需微信开发者工具验收**：未读角标、通知跳转、退款按钮和收藏预填需真实页面操作确认。
5. **匿名脚本本地 dry-run 未完成**：当前本地 Postgres 未启动，脚本入口执行到查询时报 `localhost:5432` 不可达；未操作 Docker。

### 后续待做工作 / 方向
1. 接入微信支付 V3 真实退款 API，并补齐退款回调/对账。
2. 在微信开发者工具中人工验收未读角标、通知跳转、退款按钮和收藏预填。
3. 在数据库可用且备份完成后，对旧匿名 ID 脚本先 dry-run 再选择维护窗口执行 `--apply`。

---

## 2026-07-14 - fix/p1-stability P1 稳定性与可用性修复

**发生时间**：2026-07-14 22:20 +0800
**状态**：已审查（server typecheck + server build + Prisma validate + 小程序 typecheck + 空上下文子代理 P1 复核通过）
**commit**：`eb16ffc`（squash 合并 fix/p1-stability）

### 改动概要
修复 MVP→P2 整体测试发现的 P1 问题：删除此前家教平台同步临时生成的飞书授权二维码；收藏列表按真实目标 ID 跳转，树洞收藏打开新增的树洞详情页；表白墙点赞、树洞点赞、收藏 toggle 增加并发冲突兜底，避免唯一约束/重复删除变成 500；通知、收藏、树洞分页参数统一做有限整数校验和范围钳制；岗位发布内容安全覆盖标题、描述、薪资、地点；生产环境内容安全请求失败改为 fail-closed；匿名 1v1 实时私聊必须存在 ACTIVE 匹配关系。

### 新增文件
- `apps/user-miniprogram/pages/treehole/detail/index.ts` - 树洞详情页逻辑
- `apps/user-miniprogram/pages/treehole/detail/index.wxml` - 树洞详情页结构
- `apps/user-miniprogram/pages/treehole/detail/index.wxss` - 树洞详情页样式
- `apps/user-miniprogram/pages/treehole/detail/index.json` - 树洞详情页配置

### 修改文件
- `apps/server/src/modules/chat/chat.gateway.ts` - 匿名 1v1 消息转发前校验 ACTIVE 匹配关系
- `apps/server/src/modules/confession/confession.service.ts` - 表白墙点赞 toggle 处理 P2002/P2025 并返回当前状态
- `apps/server/src/modules/favorite/favorite.controller.ts` - 收藏列表分页参数钳制
- `apps/server/src/modules/favorite/favorite.service.ts` - 收藏 toggle 处理 P2002/P2025 并返回当前状态
- `apps/server/src/modules/job/job.service.ts` - 岗位发布审核标题、描述、薪资、地点
- `apps/server/src/modules/moderation/moderation.service.ts` - 生产环境内容安全接口异常 fail-closed
- `apps/server/src/modules/notification/notification.controller.ts` - 通知分页参数钳制
- `apps/server/src/modules/treehole/treehole.controller.ts` - 树洞列表分页钳制并新增详情接口
- `apps/server/src/modules/treehole/treehole.service.ts` - 树洞详情查询与点赞 toggle 并发兜底
- `apps/user-miniprogram/app.json` - 注册树洞详情页
- `apps/user-miniprogram/pages/favorites/index.ts` - 树洞收藏跳转详情页
- `apps/user-miniprogram/pages/favorites/index.wxml` - 收藏项跳转使用 targetId
- `apps/user-miniprogram/pages/treehole/index.ts` - 树洞列表跳转详情页
- `apps/user-miniprogram/pages/treehole/index.wxml` - 树洞卡片绑定详情跳转
- `apps/user-miniprogram/services/treehole.ts` - 新增树洞详情接口
- `lark-auth-feed-next.png` - 删除临时飞书授权二维码

### 完成的工作
- 收藏列表不再把收藏记录 ID 当作帖子/岗位目标 ID 使用。
- 树洞收藏不再跳到发帖页，已接入树洞详情页和后端详情接口。
- 快速连点点赞/收藏时，服务端会吸收 P2002/P2025 并返回数据库当前状态。
- `page`、`pageSize`、`limit` 非数字、负数、过大时会回落或钳制到安全范围。
- 岗位发布的标题、薪资、地点也进入内容安全审核。
- 生产环境微信内容安全接口请求失败或 HTTP 非 2xx 时不再放行。
- 匿名私聊实时消息只允许已经随机匹配的双方互发。
- 已删除临时二维码文件，避免仓库残留无用授权材料。

### 潜在隐患
1. **树洞详情页仍需微信开发者工具人工验收**：本地 typecheck 可覆盖语法，但真实页面跳转、图片布局和 anonToken 续取仍需小程序工具验证。
2. **匿名匹配关系只有 ACTIVE 状态可发消息**：如果后续引入匹配过期/解除逻辑，需要同步维护状态流转，否则会影响旧会话继续聊天。
3. **生产内容安全 fail-closed 会提高发布失败率**：微信接口波动时用户会被要求稍后重试，后续可加短时重试或队列化审核。

### 后续待做工作 / 方向
1. squash 合并到 main 并推送。
2. 后续用微信开发者工具人工验收树洞详情页真实跳转和图片展示。

---

## 2026-07-14 · fix/p0-security-hardening P0 安全红线修复

**发生时间**：2026-07-14 21:37 +0800
**状态**：已审查（server typecheck + server build + Prisma validate + 空上下文子代理 P0 复核通过）
**commit**：`d631292`（squash 合并 fix/p0-security-hardening）

### 改动概要
修复 MVP→P2 整体审计发现的 P0 安全问题：树洞匿名 ID 不再从真实 uid 派生，且用户换匿名 token 时自动旋转旧版 `anon_<uid后6位>_...` 标识并级联更新匿名帖、匿名点赞、匹配、聊天消息和会话；生产环境支付不再 fallback mock 发布，真实微信支付下单/回调验签未接入或凭证缺失时直接抛 `90003`；生产环境内容安全缺微信凭证不再静默跳过，直接抛 `90003`。

### 新增文件
- 无

### 修改文件
- `apps/server/src/modules/treehole/treehole.service.ts` - anonId 改为随机 UUID；旧不安全 anonId 在换 token 时级联旋转
- `apps/server/src/modules/treehole/anon.guard.ts` - 拒绝旧格式 anonToken，并校验 anonId 仍是当前有效 AnonymousProfile
- `apps/server/src/modules/payment/payment.service.ts` - production 禁止 mock 下单/发布；notify production 未验签时拒绝处理；补支付配置完整性检查
- `apps/server/src/modules/moderation/moderation.service.ts` - production 缺微信凭证时 checkText/checkImage 抛 `90003`

### 完成的工作
- 树洞新匿名标识不再包含真实 uid 片段。
- 旧版不安全匿名标识在用户重新获取 anonToken 时自动旋转，并同步更新相关匿名内容和聊天索引。
- 旧版不安全 anonToken 会被 AnonGuard 拒绝，要求客户端重新获取匿名态。
- 生产环境支付未真实接入时不会创建 mock 支付成功，也不会发布岗位。
- 生产环境支付回调未接入验签时不会信任 `orderId/outTradeNo`。
- 生产环境内容安全缺 WX 凭证时不会跳过检测。
- 本地主代理已跑：server typecheck、server build、Prisma validate 均通过。
- 空上下文子代理复核：P0 主修复成立；旧 anonToken 绕过路径已通过 AnonGuard 封堵。

### 潜在隐患
1. **旧匿名数据按用户触发修复**：旧 anonToken 已被拒绝，用户重新获取 anonToken 时会旋转其旧 anonId；长期不登录用户的历史旧 anonId 仍需后续脚本批量修复。
2. **真实微信支付仍未实现**：本次只封堵生产 mock/伪造回调，真实 JSAPI 下单和 V3 回调验签仍需在配置齐全后实现。
3. **内容安全接口故障仍 fail-open**：本次修复生产缺凭证跳过问题；微信接口请求失败时仍按既有策略 fail-open，后续应改为生产可配置 fail-closed。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. 后续补真实微信支付 JSAPI 下单/V3 回调验签。
3. 后续补旧匿名 ID 批量修复脚本。

---

## 2026-07-14 · feat/feed-perf 信息流性能优化（P2）

**发生时间**：2026-07-14 20:26 +0800
**状态**：已审查（server typecheck + 小程序 typecheck + Prisma validate + 空上下文子代理静态复核通过；本地 migrate status 因 schema engine error 未完成）
**commit**：`b07156c`（squash 合并 feat/feed-perf）

### 改动概要
P2 第六项：信息流性能优化。表白墙发现流改为稳定游标（`createdAt DESC, id DESC`，base64url JSON，兼容旧 `createdAt|id` 游标）；首页发现流增加 5 分钟内存缓存（按用户 + limit 分桶，避免 `liked` 状态串用；发帖/评论/点赞变更时清缓存）；Feed/岗位列表默认 `limit=20` 并限制最大 50；Prisma 增加帖子和岗位按状态+时间查询索引；后台 7 日趋势 SQL 显式 `LIMIT 7`。

### 新增文件
- `apps/server/prisma/migrations/20260714140500_feed_perf_indexes/migration.sql` - 替换/新增 feed 与岗位列表性能索引

### 修改文件
- `apps/server/prisma/schema.prisma` - Post 增加 `[circleId,status,createdAt]` / `[status,createdAt]` 索引；JobPost 增加 `[status,createdAt]` 索引
- `apps/server/src/modules/admin/admin.module.ts` - 引入 ConfessionModule，供管理端下架帖子时清 feed 缓存
- `apps/server/src/modules/admin/admin.service.ts` - 表白墙帖子下架后清首页发现流缓存
- `apps/server/src/modules/confession/confession.module.ts` - 导出 ConfessionService
- `apps/server/src/modules/confession/confession.service.ts` - 稳定 JSON 游标 + 旧游标兼容 + 首页发现流用户分桶缓存 + 写操作清缓存
- `apps/server/src/modules/confession/dto/feed-query.dto.ts` - limit 默认 20
- `apps/server/src/modules/job/dto/job.dto.ts` - limit 默认 20
- `apps/server/src/modules/job/job.service.ts` - listPosts limit 兜底限制到 50
- `apps/server/src/modules/admin/dashboard.service.ts` - 7 日趋势 SQL 显式 LIMIT 7

### 完成的工作
- 稳定分页游标避免同一 `createdAt` 下翻页漏/重；兼容旧 `createdAt|id` 游标。
- 首页发现流缓存按用户 + limit 分桶，避免不同用户之间的 `liked` 状态串用。
- 发帖、评论、点赞/取消赞后清空首页 feed 缓存，避免点赞数和评论数长期陈旧。
- 管理端下架表白墙帖子后清空首页 feed 缓存，避免已下架内容短期残留。
- Feed / 岗位列表默认 limit 对齐 20，最大 50。
- 补充 feed 和岗位状态时间索引迁移，避免只改 schema 不落库。

### 潜在隐患
1. **内存缓存单进程有效**：多实例部署时各实例缓存不一致；MVP 可接受，后续可改 Redis 缓存。
2. **缓存清理较粗**：任何发帖/评论/点赞都会清全部 feed 缓存；实现简单但高互动场景命中率会下降。
3. **首页缓存不含 circle feed**：当前只缓存 `/posts/feed` 首页发现流，圈子列表仍实时查库。
4. **本地迁移状态检查未完成**：`prisma migrate status` 连接到本地库后返回 schema engine error，未操作 docker；已通过 `prisma validate` 和迁移文件静态复核。

### 后续待做工作 / 方向
1. 在本地数据库可用时执行索引迁移并确认 `migrate status`。
2. 提交 + squash 合并 + push。

---

## 2026-07-14 · feat/referral 分享拉新（P2）

**发生时间**：2026-07-14 01:20 +0800
**状态**：已审查（typecheck + 手动 smoke 12/12 + DB 一致 + 静态一致性全过；空上下文子代理因模型可用性暂不可用，后续补跑）
**commit**：`ec3a22b`（squash 合并 feat/referral）

### 改动概要
P2 第五项：分享拉新。`ReferralCode`（每用户唯一 6 位码，大写字母数字去易混 O/0/I/1）+ `ReferralRecord`（referrerId + refereeId 唯一，每个被邀请人只能被记录一次）+ auth.wxLogin 加 `referralCode` 入参（仅新用户首次注册生效，老用户/自邀/已记忽略，不阻断登录）+ `GET /referrals/my-code`（不存在则生成）+ `GET /referrals/my-stats`（邀请人数 + 列表）+ 小程序「邀请好友」页（邀请码 + 复制 + 分享 onShareAppMessage/onShareTimeline + 邀请记录）+ role-select 落地页识别 query.referralCode 透传登录。

### 新增文件
- `apps/server/src/modules/referral/referral.module.ts` - 模块装配
- `apps/server/src/modules/referral/referral.controller.ts` - 2 接口（my-code / my-stats）
- `apps/server/src/modules/referral/referral.service.ts` - getMyCode（懒生成+碰撞重试）/ getMyStats / onUserRegistered（登录回调）/ generateUniqueCode
- `apps/server/prisma/migrations/20260713164832_referrals/migration.sql` - referral_codes + referral_records 表
- `apps/user-miniprogram/services/referral.ts` - getMyReferralCode / getMyReferralStats + 类型
- `apps/user-miniprogram/pages/referral/index.{ts,wxml,wxss,json}` - 邀请页（码 + 复制 + 分享 + 记录）

### 修改文件
- `apps/server/prisma/schema.prisma` - 新增 ReferralCode / ReferralRecord 模型 + User 加 3 个关系
- `apps/server/src/modules/auth/dto/wx-login.dto.ts` - 加 referralCode 可选字段
- `apps/server/src/modules/auth/auth.service.ts` - 注入 ReferralService；wxLogin 判断新用户 + 带 referralCode 时调 onUserRegistered（catch 不阻断登录）
- `apps/server/src/modules/auth/auth.module.ts` - imports 加 ReferralModule
- `apps/server/src/app.module.ts` - 接入 ReferralModule
- `apps/user-miniprogram/app.ts` - loginWithRole 加 referralCode 参数透传
- `apps/user-miniprogram/utils/auth.ts` - loginWithRole 签名加 referralCode，POST body 带 referralCode
- `apps/user-miniprogram/pages/role-select/index.ts` - onLoad 接 query.referralCode；chooseRole 透传
- `apps/user-miniprogram/pages/role-select/index.wxml` - 加 referral-tip 显示
- `apps/user-miniprogram/pages/role-select/index.wxss` - .referral-tip 样式
- `apps/user-miniprogram/pages/profile/index.{ts,wxml}` - 加 goReferral 入口 + 「邀请好友」菜单项
- `apps/user-miniprogram/app.json` - 注册 pages/referral/index

### 完成的工作
- ✅ ReferralCode / ReferralRecord 模型 + 迁移（DB up to date，10 条迁移）
- ✅ 邀请码：6 位大写字母数字（去易混），碰撞重试 5 次，每用户唯一
- ✅ auth 集成：wxLogin 新用户带 referralCode -> 建邀请关联；老用户/自邀/已记 -> 静默忽略（不阻断登录）
- ✅ 2 接口：my-code（懒生成）/ my-stats（count + 50 条记录）
- ✅ 错误码 8xxxx 拉新段：80001 邀请码无效/生成失败（仅生成失败抛，无效码静默忽略）
- ✅ 小程序邀请页：邀请码展示 + 复制 + onShareAppMessage 携带 code 落地 role-select + 邀请记录列表
- ✅ role-select 落地页识别 query.referralCode + 提示 + 透传登录
- ✅ profile 加「邀请好友」入口
- typecheck（server + 小程序）双通过
- ✅ server 重启 + prisma client 重生成（referrals 路由已映射）

### 潜在隐患
1. **邀请奖励未实现**：当前仅记录邀请关系，无积分/优惠券奖励；后续按业务加 reward 发放。
2. **referralCode 静默忽略无效码**：前端分享码拼错/过期不报错；可接受（不阻断注册）。
3. **onUserRegistered 异常吞掉**：catch 后仅 logger.error，不影响登录；若 referral 表挂了邀请丢失但不影响主流程。
4. **邀请码无过期**：永久有效；如需限时奖励另加 expireAt。
5. **无防刷**：同一邀请人可邀请无数人；无频率限制；后续可加每日邀请上限。
6. **分享 path 硬编码 role-select**：onShareAppMessage path 固定 `/pages/role-select/index?referralCode=`；老用户点分享链接会进角色选择页（已登录也会），可接受。
7. **my-stats 限 50 条**：邀请超过 50 人只显示最新 50；count 是真实总数。
8. **refereeNickname/avatarUrl 暴露**：邀请记录含被邀请人昵称头像；邀请人可见，符合拉新场景。

### 后续待做工作 / 方向
1. 空上下文子代理测试（typecheck + 邀请码生成 + 新用户带码注册建关联 + 老用户带码忽略 + 自邀忽略 + my-stats + 401 + 静态一致性）。
2. 提交 + squash 合并 + push。
3. 邀请奖励发放（积分/优惠券）。
4. P2 第六项：信息流性能优化。

---

## 2026-07-14 · feat/favorites 收藏（P2）

**发生时间**：2026-07-14 00:35 +0800
**状态**：已审查（typecheck + 空上下文子代理 15/15 + DB + 静态一致性全过）
**commit**：`d842cb8`（squash 合并 feat/favorites）

### 改动概要
P2 第四项：通用收藏。`Favorite` 模型（userId+targetType+targetId 唯一，targetType 支持 `post`/`anon_post`/`job_post` 三类）+ 后端 `POST /favorites`（toggle）/ `GET /favorites`（按 type 过滤 + 分页）/ `DELETE /favorites/:id` + 跨表存在性校验（避免收藏悬空）+ 小程序「我的收藏」页（全部/表白墙/树洞/兼职 tab）+ post-detail / job-detail 加收藏按钮 + profile 入口。

### 新增文件
- `apps/server/src/modules/favorite/favorite.module.ts` - 模块装配
- `apps/server/src/modules/favorite/favorite.controller.ts` - 3 接口（toggle / list / delete）
- `apps/server/src/modules/favorite/favorite.service.ts` - toggle（@@unique 兜底）+ assertTargetExists 跨表校验 + list 分页 + delete 鉴权
- `apps/server/src/modules/favorite/dto/toggle-favorite.dto.ts` - ToggleFavoriteDto（targetType enum）+ ListFavoritesQueryDto
- `apps/server/prisma/migrations/20260713162304_favorites/migration.sql` - favorites 表 + @@unique + 索引 [userId, targetType, createdAt]
- `apps/user-miniprogram/services/favorite.ts` - toggleFavorite / listFavorites / deleteFavorite + 类型
- `apps/user-miniprogram/pages/favorites/index.{ts,wxml,wxss,json}` - 我的收藏页（4 tab + 跳转对应详情）

### 修改文件
- `apps/server/prisma/schema.prisma` - 新增 Favorite 模型 + User 加 `favorites Favorite[]` 关系
- `apps/server/src/app.module.ts` - 接入 FavoriteModule
- `apps/user-miniprogram/app.json` - 注册 pages/favorites/index
- `apps/user-miniprogram/pages/post-detail/index.ts` - import toggleFavorite + onFavorite 方法
- `apps/user-miniprogram/pages/post-detail/index.wxml` - 加 ☆/★ 收藏按钮
- `apps/user-miniprogram/pages/post-detail/index.wxss` - .fav-link / .fav-link.faved
- `apps/user-miniprogram/pages/job/detail/index.ts` - import toggleFavorite + onFavorite 方法
- `apps/user-miniprogram/pages/job/detail/index.wxml` - 加 fav-row + 收藏按钮
- `apps/user-miniprogram/pages/job/detail/index.wxss` - .fav-row / .fav-btn / .fav-btn.faved
- `apps/user-miniprogram/pages/profile/index.{ts,wxml}` - 加 goFavorites 入口 + 「我的收藏」菜单项

### 完成的工作
- ✅ Favorite 模型 + 迁移（migration 已应用，DB up to date）
- ✅ 3 接口：toggle（已收藏则删除，否则创建）/ list（按 type 过滤 + 分页 + 总数）/ delete（鉴权）
- ✅ 跨表存在性校验：targetId 必须在对应表中存在，否则 70002
- ✅ toggle 唯一约束去重：@@unique(userId, targetType, targetId) 兜底并发双击
- ✅ 错误码 7xxxx 收藏段：70001（保留，未使用）/ 70002 目标不存在 / 70003 收藏不存在
- ✅ 小程序收藏页：4 tab（全部/表白墙/树洞/兼职）+ 取消收藏 + 跳转对应详情
- ✅ 详情页加收藏按钮（post-detail / job-detail）
- ✅ profile 加「我的收藏」入口
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **未做匿名帖收藏 UI**：treehole/post 页未加收藏按钮（接口支持 anon_post）；treehole 匿名场景用户可能想收藏，本次 UI 未做，后续按需补。
2. **favorited 状态不同步**：详情页 favorite 状态未从 list 接口预填，需要点一次后才知道当前态；后续可加 `GET /favorites/check?targetType=&targetId=` 一次性查。
3. **DB 无复合索引外键**：favorite.targetId 无外键（跨 3 表），靠 service assertTargetExists 校验 + 应用层管理。删除目标时 favorite 残留（悬空），后续可加 cleanup cron。
4. **list 不分页返回 targetVo**：当前只返回 targetType/targetId/createdAt，前端跳转前不知道详情（标题/作者等），靠详情页二次请求。
5. **list pageSize 上限 50**：前端调用 50 条；够 MVP，后续分页加载。
6. **delete 鉴权按 userId**：当前用户只能删自己的收藏；已实现。
7. **toggle 返回结构不对称**：创建返回 `{favorited:true, id:'...'}`，删除返回 `{favorited:false}`；前端兼容 OK。

### 后续待做工作 / 方向
1. 空上下文子代理测试（typecheck + 9+ 项 smoke + 静态一致性 + DB 验证）。
2. 提交 + squash 合并 + push。
3. treehole 帖子详情页加收藏按钮（未做）。

---

## 2026-07-14 · feat/job-rec 兼职推荐（P2）

**发生时间**：2026-07-14 00:05 +0800
**状态**：已审查（typecheck + 空上下文子代理 10/10 + 性能 17ms 平均 + 静态一致性全过）
**commit**：`e66f3e7`（squash 合并 feat/job-rec）

### 改动概要
P2 第三项：兼职推荐。基于规则的轻量推荐：用户最近 5 个报名 → 抽 location / 城市前缀 / merchantId → 对候选池（PUBLISHED 未过期，最近 100 条）打分：同 location +5 / 同城市前缀 +3 / 多样性（新商家）+1 → score desc + createdAt desc，取 top 20。无报名历史退回按 createdAt desc top 20。小程序 job 首页加「推荐 / 全部」tab（用户角色默认推荐）。

### 修改文件
- `apps/server/src/modules/job/job.service.ts` - 加 `recommend(uid)`：最近 5 报名 → 候选池 100 条 → 打分排序 → top 20
- `apps/server/src/modules/job/job.controller.ts` - 加 `@Get('job-posts/recommend')`（声明在 `/:id` 之前避免参数路由吞掉）
- `apps/user-miniprogram/services/job.ts` - 加 `recommendJobs()` → `GET /job-posts/recommend`
- `apps/user-miniprogram/pages/job/index.ts` - 加 Tab 类型 / switchTab / loadRecommend；商家隐藏 tab；用户默认推荐
- `apps/user-miniprogram/pages/job/index.wxml` - 加 tabs（推荐/全部）+ 空态文案区分
- `apps/user-miniprogram/pages/job/index.wxss` - 加 .tabs / .tab / .tab.active 样式

### 完成的工作
- ✅ 后端 `GET /job-posts/recommend`：规则打分（location/城市前缀/多样性）+ 排序 + top 20
- ✅ 路由声明顺序正确（recommend 在 :id 前）
- ✅ 候选池限 100 条防全表扫；空 history 走 fallback
- ✅ 小程序「推荐 / 全部」tab（用户角色）；商家继续看自己岗位（无 tab）
- ✅ 推荐结果无分页（最多 20 条单次返回）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **候选池限 100 条**：超过 100 条 PUBLISHED 岗位时，热门/老岗位可能进不了候选；MVP 可接受。后续可加分页+增量或 Redis 缓存。
2. **location 解析启发式**：用空格/逗号 split 取首段做城市前缀；不规则格式（如"北京-海淀"）会归错城市。可接受。
3. **无 AB 测试**：推荐效果无法量化；后续按点击率/报名率评估。
4. **冷启动 fallback 简单**：无历史直接按时间倒序，没考虑岗位热度（review 数、apply 数）。
5. **打分不缓存**：每次请求都全跑；流量大可加 Redis 缓存（user-scored top 20）。
6. **Tab 切换 reload 全清空**：体验略生硬；后续可保留滚动位置。

### 后续待做工作 / 方向
1. 空上下文子代理测推荐：登录用户无报名 → fallback；登录用户有报名 → 含打分逻辑；未登录 → 401；其他用户可见。
2. 提交 + squash 合并 feat/job-rec。
3. P2 下一项：收藏 / 分享拉新 / 信息流性能优化。

---

## 2026-07-13 · fix/announcement-404 公告 update/delete 404 抛光

**发生时间**：2026-07-13 22:55 +0800
**状态**：已审查（typecheck + 空上下文子代理 9/9 + 静态一致性全过）
**commit**：`5dfdcfa`（squash 合并 fix/announcement-404）

### 改动概要
兑现 feat/announcements 隐患 #7：`AnnouncementService.update/delete` 在 Prisma 操作前加 `findUnique` 预检；不存在则抛 `BizException(90005, '公告不存在', HttpStatus.NOT_FOUND)`，避免走 Prisma P2025 → 全局 50000。错误码 90005 占用：90001 限流 / 90002 内容安全 / 90003 凭证缺失 / 90004 上传文件无效 / 90005 公告不存在（系统段新占位）。模式对齐 job/admin/merchant 模块（`findUnique` 预检 → BizException(NOT_FOUND)）。

### 修改文件
- `apps/server/src/modules/announcement/announcement.service.ts` - import 加 `HttpStatus` + `BizException`；update/delete 先 `findUnique` 预检，不存在抛 90005

### 完成的工作
- ✅ `update(id, dto)` 不存在 → HTTP 404 + `{code:90005, message:'公告不存在'}`
- ✅ `delete(id)` 不存在 → HTTP 404 + `{code:90005, message:'公告不存在'}`
- ✅ 沿用代码库惯例（findUnique 预检，非 catch Prisma 异常）

### 潜在隐患
1. **TOCTOU**：findUnique 后 update/delete 之间被并发删除，会回退到 P2025 → 50000。概率极低（管理员手动操作），可接受。

### 后续待做工作 / 方向
1. 空上下文子代理测 404 用例（PUT 不存在 ID → 404/90005；DELETE 不存在 ID → 404/90005；正常 PUT/DELETE → 200）。
2. 提交 + squash 合并 fix/announcement-404 + push（批量推时一起）。

---

## 2026-07-13 · feat/announcements 公告推送（P2）

**发生时间**：2026-07-13 22:25 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 16/16 + DB + 静态一致性全过）
**commit**：`eb0d90e`（squash 合并 feat/announcements）

### 改动概要
P2 第二项：公告推送。`Announcement` 模型 + 后端模块（listActive 公开/listAll 管理/create/update/delete）+ 小程序管理端「公告」tab（CRUD + 启用/停用）+ 表白墙首页顶部公告栏（swiper 自动轮播）。管理员 `AdminGuard` 保护管理接口，普通登录用户可见 active 公告。

### 新增文件
- `apps/server/prisma/migrations/20260713221631_announcements/migration.sql` - 建 announcements 表（id/title/content/active/createdAt/updatedAt）
- `apps/server/src/modules/announcement/announcement.module.ts` - @Global 模块
- `apps/server/src/modules/announcement/announcement.controller.ts` - GET 公开 + GET(all)/POST/PUT/DELETE @AdminGuard
- `apps/server/src/modules/announcement/announcement.service.ts` - listActive(限 10)/listAll/create/update/delete
- `apps/server/src/modules/announcement/dto/announcement.dto.ts` - CreateAnnouncementDto / UpdateAnnouncementDto（class-validator）
- `apps/user-miniprogram/services/announcement.ts` - listAnnouncements / listAllAnnouncements / createAnnouncement / updateAnnouncement / deleteAnnouncement + AnnouncementVo / AdminAnnouncementVo

### 修改文件
- `apps/server/prisma/schema.prisma` - 新增 Announcement 模型（active 默认 true，updatedAt @updatedAt）
- `apps/server/src/app.module.ts` - imports 加 AnnouncementModule
- `apps/user-miniprogram/pages/admin/index.ts` - Tab 加 announce + data.announcements/annTitle/annContent + createAnn/toggleAnn/deleteAnn/onAnnInput
- `apps/user-miniprogram/pages/admin/index.wxml` - tabs 加「公告」+ 公告 tab 渲染（form：title+content input + 发布按钮 / 列表：标题/状态/操作）
- `apps/user-miniprogram/pages/admin/index.wxss` - 加 .ann-form/.ann-input/.ann-textarea/.ann-submit 样式
- `apps/user-miniprogram/pages/confession/index.ts` - onShow 拉 listAnnouncements；data.announcements
- `apps/user-miniprogram/pages/confession/index.wxml` - 顶部加 .announce-bar（swiper 自动轮播 active 公告）
- `apps/user-miniprogram/pages/confession/index.wxss` - .announce-bar/.announce-swiper/.announce-text 样式（米色背景 #fff8e6 + 警示黄文字）

### 完成的工作
- ✅ Announcement 模型 + 迁移（migration 已应用，DB schema up to date）
- ✅ 后端 5 个接口：GET 公开（限 10 条 active，登录即可）+ 4 个 @AdminGuard 管理接口
- ✅ AdminGuard 控制管理接口（继承 auth-role 模块守卫）
- ✅ 小程序管理端「公告」tab：发布表单 + 列表（启用/停用 toggle + 删除确认）
- ✅ 表白墙首页顶部公告栏：active 公告 swiper 自动轮播（4s 间隔），米色警示样式
- ✅ Prisma client 已重新生成（含 Announcement 模型）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **公告内容无内容安全**：管理员发布的标题/内容未走 checkText；管理员可信可接受，但生产可加。
2. **公开接口无限流**：GET /announcements 走全局 100 req/min，公告读取频率不高可接受；如需单独限流再 @Throttle。
3. **删除硬删**：delete 是真删（含历史），如需审计可加 status=DELETED 字段。
4. **updatedAt 暴露**：AdminAnnouncementVo 含 updatedAt 给前端，但前端未使用（创建时间足够）。
5. **小程序公告加载静默失败**：confession onShow 用 `.catch(() => {})` 吞错，公告接口失败不影响主流程；缺可见性。
6. **公告栏 swiper 性能**：active 公告多时 swiper 全量渲染；take=10 限制下可接受。
7. **PUT 不存在 ID 走 500**：子代理 b16 测出 Prisma P2025 未在 service 层捕获，被全局异常过滤器转 `code:50000`；契约 500 可接受但体验差；后续 service.update/delete 加 try/catch P2025 → BizException(90004, '公告不存在', 404)。

### 后续待做工作 / 方向
1. 提交 + squash 合并 feat/announcements + push。
2. service.update/delete 捕获 Prisma P2025 → BizException(90004, 404)。
3. P2 下一项：兼职推荐 / 收藏 / 分享拉新 / 家教平台同步 / 信息流性能优化。

---

## 2026-07-13 · feat/dashboard 数据看板（P2）

**发生时间**：2026-07-13 22:10 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/dashboard）

### 改动概要
P2 第一项：数据看板。`GET /admin/stats` 返回总览（用户/商家/帖子/岗位/报名/评价）+ 今日数据 + 近 7 天趋势（raw SQL generate_series 按日分组）。小程序管理端加「数据看板」tab（总览卡片 + 今日 + 趋势表）。

### 新增文件
- `apps/server/src/modules/admin/dashboard.service.ts` - getStats（12 项 count Promise.all）+ getTrend（$queryRaw generate_series 按日分组 posts/applications/newUsers）

### 修改文件
- `apps/server/src/modules/admin/admin.controller.ts` - 加 `GET /admin/stats`；注入 DashboardService
- `apps/server/src/modules/admin/admin.module.ts` - providers 加 DashboardService
- `apps/user-miniprogram/services/admin.ts` - 导出 getStats + DashboardStats 接口
- `apps/user-miniprogram/pages/admin/index.ts` - 加 stats tab + load getStats
- `apps/user-miniprogram/pages/admin/index.wxml` - 加数据看板 UI（总览 6 卡片 + 今日 4 卡片 + 近 7 天趋势表）
- `apps/user-miniprogram/pages/admin/index.wxss` - 加 stats-grid/stat-card/trend-table 样式

### 完成的工作
- ✅ `GET /admin/stats`：overview（8 项总数）+ today（4 项今日新增）+ trend（近 7 天每日 posts/applications/newUsers）
- ✅ raw SQL `generate_series` 按日分组趋势（Prisma 不原生支持日期分组）
- ✅ 小程序数据看板 UI（总览卡片网格 + 今日 + 趋势表三栏）
- ✅ 空上下文子代理测试全过（5 步 a-e：stats 返回全数字 + trend 末行今天 + 非管理员 403 + 401 + 静态一致性）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **raw SQL 性能**：generate_series + 子查询在大数据量下慢；生产需加索引或物化视图。
2. **无 DAU 统计**：当前无 DAU（日活用户）统计（需 session/访问日志表）；trend 的 newUsers 是新注册数非 DAU。
3. **趋势 date 含时区**：raw SQL 返回 `YYYY-MM-DD HH:mm:ss+00`（含时区）；前端展示取前 10 位即可。
4. **无缓存**：每次查询全量 count；高频访问需加 Redis 缓存（feat/perf 时做）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. P2 下一项：`feat/announcements`（公告推送）。

---

## 2026-07-13 · feat/admin-enhance 批量审核 + 审核理由回填（P1 完成）

**发生时间**：2026-07-13 21:58 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过，含审核留痕 DB 验证）
**commit**：未提交（待 squash 合并 feat/admin-enhance）

### 改动概要
P1 第五项（最后一项）管理端增强：审核操作创建 ModerationRecord 留痕（reason + reviewerId）+ 批量审核商家 + admin/queue 含举报记录 + 小程序审核弹窗输入理由 + 批量通过按钮。**P1 全部 5 项完成。**

### 新增文件
- `apps/server/src/modules/admin/dto/batch-merchant.dto.ts` - BatchMerchantDto { ids, action, reason? }

### 修改文件
- `apps/server/src/modules/admin/admin.service.ts` - approveMerchant/rejectMerchant/takedownPost/takedownAnonPost 加 reviewerId + reason 参数，创建 ModerationRecord 留痕；新增 batchMerchants；getQueue 加 reports（ModerationRecord PENDING）
- `apps/server/src/modules/admin/admin.controller.ts` - 各审核端点从 req.body 取 reason + req.user.uid 作 reviewerId；加 `POST /admin/merchants/batch`；修复 UseGuards import
- `apps/user-miniprogram/services/admin.ts` - approveMerchant/rejectMerchant/takedownPost/takedownAnonPost 加 reason 参数；导出 batchMerchants；AdminQueueVo 加 reports
- `apps/user-miniprogram/pages/admin/index.ts` - 审核操作用 wx.showModal editable 获取理由；新增 batchApprove
- `apps/user-miniprogram/pages/admin/index.wxml` - 加批量通过按钮 + 举报记录区
- `apps/user-miniprogram/pages/admin/index.wxss` - 加 batch-btn + empty-tip 样式

### 完成的工作
- ✅ 审核留痕：approve/reject/takedown 创建 ModerationRecord（targetType + targetId + reason + status + reviewerId）
- ✅ 批量审核：`POST /admin/merchants/batch { ids, action, reason? }` -> 循环 approve/reject + 留痕
- ✅ admin/queue 含举报记录：ModerationRecord PENDING 列表（用户举报 + 待处理）
- ✅ 小程序：审核操作弹窗输入理由（wx.showModal editable）+ 批量通过待审核商家按钮 + 举报记录展示
- ✅ 空上下文子代理测试全过（13 步 a-m：拒绝带理由+DB验证 / 通过带理由 / 批量审核 processed=2 / queue 含举报 / 下架带理由 / 非管理员 403 / 401）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **批量审核串行**：for 循环逐个处理，大量商家时慢；可改 Promise.all（但需注意角色操作顺序）。
2. **举报无去重**：同一用户可多次举报同一内容，创建多条 ModerationRecord。
3. **审核留痕无 reviewerId 校验**：reviewerId 直接从 token uid 取，假设是管理员（AdminGuard 保证）。
4. **ModerationRecord 不自动清理**：已处理的记录留在表里（status 非 PENDING 不在 queue 展示，但数据累积）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. **P1 全部完成** 🎉：rate-limit + notifications + social-enhance + job-enhance + admin-enhance。
3. P2（里程碑三）：数据看板 / 公告推送 / 兼职推荐 / 收藏 / 分享拉新 / 家教平台同步 / 信息流性能优化。

---

## 2026-07-13 · feat/job-enhance 商家评价查看 + 订单记录（P1）

**发生时间**：2026-07-13 11:19 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/job-enhance）

### 改动概要
P1 第四项兼职增强：商家评价查看（GET /merchant/reviews 跨岗位评价列表）+ 商家订单记录（GET /merchant/orders 付费发布历史）。小程序评价管理页 + 订单记录页 + merchant/profile 入口。

### 修改文件
- `apps/server/src/modules/merchant/merchant.service.ts` - 加 `getMerchantReviews`（两步查询：先岗位 IDs 再评价）+ `getMerchantOrders`（订单 + 岗位标题 Map 映射）
- `apps/server/src/modules/merchant/merchant.controller.ts` - 加 `GET /merchant/reviews` + `GET /merchant/orders`
- `apps/user-miniprogram/services/merchant.ts` - 导出 `getMerchantReviews` + `getMerchantOrders` + MerchantReviewVo + MerchantOrderVo
- `apps/user-miniprogram/pages/merchant/reviews/index.{ts,wxml,wxss,json}` - 评价管理页（评分星 + 内容 + 岗位标题 + 评价人）
- `apps/user-miniprogram/pages/merchant/orders/index.{ts,wxml,wxss,json}` - 订单记录页（金额 + 档位 + 状态 + 岗位标题）
- `apps/user-miniprogram/pages/merchant/profile/index.{ts,wxml,wxss}` - 加 goReviews + goOrders 入口（APPROVED 商家可见）
- `apps/user-miniprogram/app.json` - 注册 merchant/reviews + merchant/orders

### 完成的工作
- ✅ `GET /merchant/reviews`：商家跨岗位评价列表（rating + content + jobPostTitle + reviewerNickname）
- ✅ `GET /merchant/orders`：商家付费发布订单记录（amount + duration + status + jobPostTitle + paidAt）
- ✅ 两步查询规避 Prisma 嵌套 filter 类型推断问题 + PaymentOrder 无 jobPost 关系问题
- ✅ 小程序评价管理页 + 订单记录页 + merchant/profile 入口
- ✅ 空上下文子代理测试全过（13 步 a-m：发岗->付费->报名->录用->完成->评价->查评价->查订单 + 非商家 60002 + 401）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **PaymentOrder 无 jobPost 关系**：两步查询（订单 + 岗位标题 Map）规避；后续可加 schema 关系简化。
2. **评价无分页**：商家评价/订单一次返回全量（findMany 无分页）；大量数据时需分页。
3. **评价/订单需商家身份**：用 user token 调（非 merchant token），后端按 uid 查 Merchant；非商家 60002。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. 评价/订单分页。
3. PaymentOrder 加 jobPost 关系（schema 简化）。
4. P1 最后一项：`feat/admin-enhance`（批量审核 + 审核理由）。

---

## 2026-07-13 · feat/social-enhance 表白墙举报 + 树洞匿名点赞（P1）

**发生时间**：2026-07-13 10:49 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/social-enhance）

### 改动概要
P1 第三项社交增强：表白墙举报（POST /posts/:id/report -> ModerationRecord）+ 树洞匿名点赞（AnonPostLike 模型 + toggle + likeCount + liked 状态）。

### 新增文件
- `apps/server/src/modules/confession/dto/report.dto.ts` - ReportDto { reason? }
- `apps/server/prisma/migrations/<timestamp>_anon_post_likes/migration.sql` - likeCount 列 + anon_post_likes 表

### 修改文件
- `apps/server/prisma/schema.prisma` - AnonymousPost 加 `likeCount` + `likes` 关系；新增 `AnonPostLike` 模型（@@unique [postId, anonId]）
- `apps/server/src/modules/confession/confession.service.ts` - 加 `reportPost`（创建 ModerationRecord）；import HttpStatus
- `apps/server/src/modules/confession/confession.controller.ts` - 加 `POST /posts/:id/report`；import ReportDto
- `apps/server/src/modules/treehole/treehole.service.ts` - `listPosts` 加 anonId 参数 + include likes；`toVo` 加 likeCount + liked；新增 `toggleAnonPostLike`（transaction 同步 likeCount）
- `apps/server/src/modules/treehole/treehole.controller.ts` - GET /posts 传 anonId；加 `POST /treehole/posts/:id/like`（@Throttle 10/min）
- `apps/user-miniprogram/services/confession.ts` - 导出 `reportPost`
- `apps/user-miniprogram/services/treehole.ts` - AnonPostVo 加 likeCount + liked；导出 `toggleAnonPostLike`
- `apps/user-miniprogram/pages/post-detail/index.{ts,wxml,wxss}` - 加 `onReport` 举报按钮
- `apps/user-miniprogram/pages/treehole/index.{ts,wxml,wxss}` - 重写 wxml（含点赞按钮）；加 `onLike` 乐观更新

### 完成的工作
- ✅ 表白墙举报：`POST /posts/:id/report { reason? }` -> 创建 ModerationRecord（targetType=post, PENDING），管理员审核队列可见
- ✅ 树洞匿名点赞：`POST /treehole/posts/:id/like`（anonToken 鉴权）-> toggle（AnonPostLike @@unique 去重，transaction 同步 likeCount）
- ✅ 树洞广场列表含 likeCount + liked（按当前 anonId 查 AnonPostLike）
- ✅ 小程序：post-detail 举报按钮（弹窗确认）；treehole 广场点赞按钮（乐观更新）
- ✅ 空上下文子代理测试全过（16 步 a-p：举报+DB验证 / 点赞 toggle+多用户+liked状态 + 静态一致性）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **举报无防重**：同一用户可多次举报同一帖子（创建多条 ModerationRecord）；P1 后续加唯一约束或前端去重。
2. **AdminService 未展示举报**：admin/queue 返回帖子但不查 ModerationRecord；P1 后续在 admin 队列展示举报记录。
3. **点赞 race**：find-then-act 非原子，并发可能 P2002；可改 upsert 或捕获 P2002。
4. **treehole/index.wxml 之前是占位**：feat/treehole 时 wxml 写入未生效（原因不明），本次重写修复。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push（与 feat/notifications 一起推）。
2. 举报防重（ModerationRecord @@unique [targetType, targetId, reviewerId]）。
3. admin 队列展示举报记录。
4. P1 下一项：`feat/job-enhance`（商家评价查看/资料/订单）。

---

## 2026-07-13 · feat/notifications 站内通知（P1）

**发生时间**：2026-07-13 10:06 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过，含通知端到端）
**commit**：未提交（待 squash 合并 feat/notifications）

### 改动概要
P1 第二项：站内通知。Notification 模型 + 全局 NotificationService（create/list/markRead/markAllRead）+ 集成 JobService（报名->通知商家、录用/完成->通知用户）+ 小程序消息中心页（列表 + 未读数 + 标记已读/全部已读）。

### 新增文件
- `apps/server/src/modules/notification/notification.service.ts` - create（内部调用）/ list（分页+未读数）/ markRead / markAllRead + NotificationType 常量
- `apps/server/src/modules/notification/notification.controller.ts` - `GET /notifications`、`POST /notifications/:id/read`、`POST /notifications/read-all`
- `apps/server/src/modules/notification/notification.module.ts` - @Global，exports NotificationService
- `apps/server/prisma/migrations/<timestamp>_notifications/migration.sql`
- `apps/user-miniprogram/services/notification.ts` - listNotifications/markNotificationRead/markAllNotificationsRead
- `apps/user-miniprogram/pages/notifications/index.{ts,wxml,wxss,json}` - 消息中心页（未读数 + 列表 + 标记已读 + 全部已读）

### 修改文件
- `apps/server/prisma/schema.prisma` - 新增 Notification 模型（userId/type/title/content/targetType?/targetId?/read/createdAt + 索引 [userId,read,createdAt]）
- `apps/server/src/app.module.ts` - 接入 NotificationModule
- `apps/server/src/modules/job/job.service.ts` - 注入 NotificationService；apply 创建 job_apply 通知商家；transition accept 创建 job_accept、complete 创建 job_complete 通知用户
- `apps/user-miniprogram/app.json` - 注册 pages/notifications
- `apps/user-miniprogram/pages/profile/index.{ts,wxml}` - 加 goNotifications「消息中心」入口

### 完成的工作
- ✅ Notification 模型 + 迁移（notifications 表 + 索引 [userId,read,createdAt]）
- ✅ NotificationService（@Global）：create 供其他模块注入；list 分页 + unreadCount；markRead/markAllRead
- ✅ 3 接口：`GET /notifications?unreadOnly=&page=&pageSize=`、`POST /notifications/:id/read`、`POST /notifications/read-all`
- ✅ 集成 JobService：报名->job_apply 通知商家；录用->job_accept 通知用户；完成->job_complete 通知用户
- ✅ 小程序消息中心页（未读数 + 列表 + 点击标记已读 + 全部已读）+ profile 入口
- ✅ 空上下文子代理测试全过（17 步 a-q：报名->通知->录用->通知->完成->通知->标记已读->全部已读 + 401 + DB + 静态一致性）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **无实时推送**：通知靠 HTTP 拉取（用户进消息中心才看到）；P1 后续可加 ws 实时推送（ChatGateway 已有 ws 基础）。
2. **无微信订阅消息**：仅站内通知；微信订阅消息需另接（P1 后续）。
3. **通知无 target 跳转**：消息中心点击仅标记已读，不跳转对应详情页；P1 后续按 targetType/targetId 跳转。
4. **AdminService 未集成通知**：帖子下架未通知作者；P1 后续加。
5. **通知无清理**：旧通知不自动清理；P1 后续加定期清理 cron。
6. **unreadCount 实时性**：profile 页未拉 unreadCount（无角标）；P1 后续在 profile onShow 拉取显示角标。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. 通知 ws 实时推送（ChatGateway 广播）。
3. 微信订阅消息接入。
4. 通知点击跳转对应详情页（按 targetType/targetId）。
5. AdminService 下架通知作者。
6. profile 页未读角标。
7. P1 下一项：`feat/social-enhance`（社交增强）。

---

## 2026-07-12 · feat/rate-limit 接口限流（P1）

**发生时间**：2026-07-12 14:58 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过，含 429 实际触发验证）
**commit**：未提交（待 squash 合并 feat/rate-limit）

### 改动概要
P1 第一项：接口限流。@nestjs/throttler 全局 100 req/min + 敏感接口 @Throttle 覆盖（wx-login/refresh 5/min，发帖/评论 5/min，匹配 10/min，上传 10/min）。429 -> 业务码 90001 + Retry-After 头（API 规范 §8）。

### 修改文件
- `apps/server/src/app.module.ts` - `ThrottlerModule.forRoot([{ ttl: 60_000, limit: 100 }])` + `ThrottlerGuard` 作为 APP_GUARD（先于 JwtAuthGuard）
- `apps/server/src/common/filters/all-exceptions.filter.ts` - 429 -> `{ code: 90001, message: '请求过于频繁，请稍后重试', traceId }` + `Retry-After: 60` 头
- `apps/server/src/modules/auth/auth.controller.ts` - wx-login + refresh `@Throttle({ default: { ttl: 60_000, limit: 5 } })`
- `apps/server/src/modules/confession/confession.controller.ts` - createPost + createComment @Throttle 5/min
- `apps/server/src/modules/treehole/treehole.controller.ts` - createPost @Throttle 5/min + match @Throttle 10/min
- `apps/server/src/modules/upload/upload.controller.ts` - upload @Throttle 10/min
- `apps/server/package.json` + `pnpm-lock.yaml` - 加 `@nestjs/throttler` ^6.5.0

### 完成的工作
- ✅ 全局限流：每 IP 100 req/min（ThrottlerGuard 作为 APP_GUARD，先于 JwtAuthGuard）
- ✅ 敏感接口覆盖：wx-login/refresh 5/min、发帖/评论 5/min、匹配 10/min、上传 10/min
- ✅ 429 响应：`{ code: 90001, message: '请求过于频繁，请稍后重试', traceId }` + `Retry-After: 60` 头
- ✅ 空上下文子代理测试全过：连发 6 次 wx-login -> 前5次 201(code:0)，第6次 429(code:90001 + Retry-After)；其他接口（/auth/me、/circles、/posts/feed）不受影响
- typecheck 通过

### 潜在隐患
1. **内存限流**：ThrottlerGuard 默认内存存储，多实例不共享；生产多实例需换 Redis 存储（@nestjs/throttler-storage-redis）。
2. **全局 100/min 可能偏紧**：小程序一次页面加载可能并发多个请求；如误触可调高。
3. **@Public 接口也被限流**：ThrottlerGuard 不区分 @Public，wx-login/refresh/notify 等白名单接口也限流（合理，防刷）。
4. **无 IP 白名单**：未排除内网/管理 IP；生产可加 trusted proxy 配置。
5. **限流粒度按 IP**：NAT 后多用户共享 IP 可能误触；生产需配 `X-Forwarded-For`（nginx 已设）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. 生产多实例：换 `@nestjs/throttler-storage-redis`。
3. P1 下一项：`feat/notifications`（站内通知）。

---

## 2026-07-12 · feat/cron-deploy 到期下架 cron + 部署配置（MVP 收尾）

**发生时间**：2026-07-12 12:28 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过，含到期下架端到端）
**commit**：未提交（待 squash 合并 feat/cron-deploy）

### 改动概要
MVP 收尾：岗位到期下架定时任务（@nestjs/schedule @Cron 每小时扫 PUBLISHED+expireAt<now -> EXPIRED + dev 触发端点）+ 部署配置（Dockerfile / docker-compose.prod.yml / nginx.conf / PM2 ecosystem / 部署.md）。**MVP 全部 7 个 Phase 完成。**

### 新增文件
- `apps/server/src/modules/job/job.scheduler.ts` - `@Cron('0 * * * *')` expireJobPosts（updateMany PUBLISHED+expireAt<now -> EXPIRED）
- `Dockerfile` - 单阶段构建（pnpm install + 共享包 build + prisma generate + server build -> node dist/main.js）
- `docker-compose.prod.yml` - 生产全栈（postgres + redis + server + nginx）
- `nginx.conf` - HTTP /api/ 反代 + WebSocket /ws upgrade + 健康检查
- `ecosystem.config.js` - PM2 进程管理（不用 Docker 时的替代）
- `docs/开发记录/部署.md` - 部署指南（Docker Compose + PM2 两种方式 + 环境变量 + Nginx + cron + 小程序）

### 修改文件
- `apps/server/src/app.module.ts` - 接入 `ScheduleModule.forRoot()`
- `apps/server/src/modules/job/job.module.ts` - providers 加 `JobScheduler`
- `apps/server/src/modules/job/job.controller.ts` - 加 `POST /job-posts/expire-now`（dev only，prod 拒绝）；注入 `JobScheduler`
- `apps/server/package.json` + `pnpm-lock.yaml` - 加 `@nestjs/schedule` ^6.1.3

### 完成的工作
- ✅ `JobScheduler`：`@Cron('0 * * * *')` 每小时扫描 PUBLISHED + expireAt<now -> updateMany EXPIRED（幂等）
- ✅ `POST /job-posts/expire-now`（dev only）：手动触发到期下架（测试用，prod 禁止）
- ✅ `Dockerfile`：单阶段构建 server（含共享包 + Prisma generate）
- ✅ `docker-compose.prod.yml`：生产全栈（server + postgres + redis + nginx）
- ✅ `nginx.conf`：HTTP 反代 `/api/` + WebSocket upgrade `/ws`
- ✅ `ecosystem.config.js`：PM2 配置（dev/prod env）
- ✅ `docs/开发记录/部署.md`：完整部署指南（Docker + PM2 + 环境变量 + Nginx + cron + 小程序）
- ✅ 空上下文子代理测试全过（10 步 a-j：发岗->付费->置过期->expire-now->EXPIRED + 幂等 + 静态一致性）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **cron 单实例**：@Cron 在单进程内运行，多实例会重复执行（updateMany 幂等但重复扫描）；生产多实例需分布式锁或只跑一个实例。
2. **Dockerfile 单阶段**：未做多阶段优化（镜像偏大）；生产可优化为 multi-stage build 减小镜像。
3. **nginx 无 TLS**：MVP 仅 HTTP，生产需加 443 + wss + 证书（certbot 或手动）。
4. **CHAT_WS_URL 配置**：生产需配 `wss://domain/ws`（经 nginx 代理），dev 用 `ws://localhost:3001`。
5. **cron 每小时**：过期岗位最长 1 小时后才下架；如需更及时可缩短间隔。
6. **Dockerfile 未含 miniprogram**：小程序不需部署（微信开发者工具上传）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. **MVP 完成** 🎉：全 7 Phase 跑通（auth/表白墙/兼职闭环/树洞/管理端/部署）。
3. 生产化：真实 WX 凭证 + 微信支付商户证书 + COS + TLS 证书 + 管理员真实 openid。
4. P1 迭代：兴趣匹配/历史会话/通知/退款/搜索筛选等（见路线图里程碑二）。

---

## 2026-07-12 · feat/admin 管理员审核/单价/封禁

**发生时间**：2026-07-12 11:37 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/admin）

### 改动概要
管理员功能：审核队列（商家/表白墙帖/树洞帖）+ 商家审核 approve/reject + 帖子下架 + PricingConfig 查看/更新 + 用户封禁（soft delete + auth 登录拦截 10005）。@AdminGuard 校验 role=ADMIN。小程序管理员页（审核队列/单价配置双 tab）+ profile 管理后台入口。

### 新增文件
- `apps/server/src/modules/admin/admin.service.ts` - getQueue/approveMerchant/rejectMerchant/takedownPost/takedownAnonPost/getPricing/updatePricing/banUser
- `apps/server/src/modules/admin/admin.controller.ts` - `@UseGuards(AdminGuard)` controller 级；8 接口
- `apps/server/src/modules/admin/admin.module.ts`
- `apps/server/src/modules/admin/dto/update-pricing.dto.ts`
- `apps/user-miniprogram/services/admin.ts` - 接口封装
- `apps/user-miniprogram/pages/admin/index.{ts,wxml,wxss,json}` - 管理员页（审核队列/单价配置双 tab）

### 修改文件
- `apps/server/src/app.module.ts` - 接入 AdminModule
- `apps/server/src/modules/auth/auth.service.ts` - wxLogin 加 `deletedAt` 封禁检查（10005）
- `apps/user-miniprogram/app.json` - 注册 pages/admin
- `apps/user-miniprogram/pages/profile/index.{ts,wxml}` - 加 `goAdmin` 入口（currentRole==='ADMIN' 条件显示）

### 完成的工作
- ✅ `GET /admin/queue`：审核队列（商家全量 + 表白墙帖 + 树洞帖，各 50 条）
- ✅ `POST /admin/merchants/:id/approve|reject`：商家审核（approve 加 MERCHANT 角色，reject 移除）
- ✅ `POST /admin/posts/:id/takedown` + `POST /admin/anon-posts/:id/takedown`：帖子下架（REJECTED）
- ✅ `GET /admin/pricing` + `PUT /admin/pricing`：PricingConfig 查看/更新
- ✅ `POST /admin/users/:id/ban`：用户封禁（soft delete `deletedAt`）
- ✅ auth wxLogin 封禁检查：`deletedAt` 非空 -> 10005 拒绝登录
- ✅ `@AdminGuard`（controller 级）：非管理员 403/10003
- ✅ 小程序管理员页（审核队列 + 单价配置双 tab）+ profile 管理后台入口
- ✅ 空上下文子代理测试全过（16 接口 smoke a-p + 静态一致性 6/6）
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **审核队列返回全量内容**：非仅 PENDING（dev 下帖子/商家 auto-APPROVED，队列展示全部供管理员下架/驳回）；生产应按 PENDING 过滤。
2. **banUser 仅 soft delete**：设 deletedAt，已有 token 仍有效至过期（2h）；如需立即失效需 Redis 黑名单。
3. **无审核记录**：ModerationRecord 表未使用（审核动作直接改内容表 status）；P1 补审核留痕。
4. **树洞帖下架需 anonToken 上下文**：admin 接口用 access token，但树洞帖是匿名的；admin queue 返回 anonPosts 供下架，不需 anonToken。
5. **单价修改即时生效**：已发布岗位单价锁定不变（PaymentOrder 已记录），新发布用新价。
6. **admin 无独立审核理由**：reject/takedown 无 reason 字段；P1 加。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. 审核队列按 PENDING 过滤（生产）。
3. ModerationRecord 审核留痕（reason + reviewerId）。
4. banUser Redis 黑名单（立即失效旧 token）。
5. `feat/cron-deploy`：最后一个分支（到期下架 cron + 部署）。

---

## 2026-07-12 · feat/treehole 树洞匿名动态 + 1v1 匹配 + 派对房

**发生时间**：2026-07-12 11:01 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过，含匿名红线三层面验证）
**commit**：未提交（待 squash 合并 feat/treehole）

### 改动概要
树洞匿名社交（第二条 MVP 主线）：匿名 token 签发（anonId，不含 uid，AnonGuard 独立鉴权）/ 匿名动态广场 CRUD / 1v1 随机匹配（内存队列 -> ChatMatch + IM 凭证）/ 多人派对房（ChatGateway room）。匿名红线三层面守住：DB 表 0 uid / JWT payload 0 uid / VO 0 uid。

### 新增文件
- `apps/server/src/modules/treehole/anon.guard.ts` - AnonGuard：校验 anonToken（type=anon），设 req.user.uid=anonId（不含真实 uid）
- `apps/server/src/modules/treehole/treehole.service.ts` - getAnonymousToken / createPost / listPosts / match（内存队列）/ joinParty
- `apps/server/src/modules/treehole/treehole.controller.ts` - `POST /treehole/anonymous-token`（access token）、`POST/GET /treehole/posts`、`POST /treehole/match`、`POST /treehole/party/join`（均 @Public+AnonGuard）
- `apps/server/src/modules/treehole/treehole.module.ts`
- `apps/server/src/modules/treehole/dto/create-anon-post.dto.ts`
- `apps/user-miniprogram/services/treehole.ts` - anonToken 管理 + 接口封装
- `apps/user-miniprogram/pages/treehole/{post,chat,party}/index.{ts,wxml,wxss,json}` - 发帖/1v1匹配聊/派对房 3 新页
- `apps/user-miniprogram/pages/treehole/index.{ts,wxml,wxss,json}` - tabBar 占位页改为匿名广场

### 修改文件
- `apps/server/src/app.module.ts` - 接入 TreeholeModule
- `apps/server/src/modules/auth/types.ts` - JwtPayload.type 加 `'anon'`
- `apps/user-miniprogram/services/im.ts` - 加 `sendWsMessage`（ws-only 1v1 发消息，供树洞匿名聊）
- `apps/user-miniprogram/app.json` - 注册 treehole/post、treehole/chat、treehole/party

### 完成的工作
- ✅ `POST /treehole/anonymous-token`：find/create AnonymousProfile（userId->anonId 后台追溯），签 anonToken（JWT type=anon, 7d，**不含 uid**）
- ✅ AnonGuard：树洞接口 @Public 跳过 JwtAuthGuard + @UseGuards(AnonGuard) 校验 anonToken；access token 被拒（30001）
- ✅ 匿名动态广场：POST/GET posts（checkText + checkImage），游标分页
- ✅ 1v1 随机匹配：内存队列撮合 -> ChatMatch + ImService.getImCredential(anonId)（ws 连接，anonId 作 loginUserId）
- ✅ 多人派对房：joinParty 返回 roomId + imCredential，客户端 ws joinRoom + room-msg 群聊
- ✅ 小程序 4 页：广场（帖子列表+入口）/ 发帖 / 1v1 匹配聊（ws sendWsMessage）/ 派对房（ws joinRoom+sendRoomMessage）
- ✅ 空上下文子代理测试全过（12 接口 smoke a-l + DB 红线 + JWT payload decode + 静态一致性）
- typecheck（server + 小程序）双通过
- **匿名红线三层面验证**：DB anonymous_posts 无 user_id / JWT payload `{anonId, type:'anon'}` 无 uid / VO 返回无 uid

### 潜在隐患
1. **内存匹配队列**：单实例内存，多实例不共享；生产用 Redis list（LPUSH/RPOP）。
2. **匹配 waiting 无回调**：waiting 时客户端需轮询或提示稍后再试；P1 可加 ws 推送通知匹配成功。
3. **1v1 聊天不落库**：树洞匿名 1v1 聊消息纯 ws 实时不落库（匿名"阅后即焚"）；如需历史需另存 ChatMessage（anonId，不存 uid）。
4. **派对房固定单房**：MVP 固定 `treehole-party-main`；P1 可扩展多主题房。
5. **匿名马甲不可换**：AnonymousProfile 创建后 anonId 固定；功能方案说"每月可换1次"，P1 加。
6. **无举报/下架**：匿名帖无举报入口；feat/admin 审核队列覆盖。
7. **anonToken 7d 过期**：过期后需重新 /treehole/anonymous-token（用 access token）；前端 treehole service 应处理 30001 自动刷新。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push（与 feat/payment 一起推）。
2. 匹配用 Redis list（多实例）。
3. 匹配成功 ws 推送通知（waiting 端自动感知）。
4. 匿名马甲更换（每月1次）。
5. `feat/admin` 审核队列覆盖树洞帖。
6. `feat/cron-deploy` 部署含 ws 服务端。

---

## 2026-07-11 · feat/payment 付费发布 + 微信支付

**发生时间**：2026-07-11 00:02 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/payment）

### 改动概要
付费发布：按 `PricingConfig` 服务端计价下单（不信前端金额）+ 微信支付下单 + 回调验签置 PUBLISHED + expireAt。dev mock 直接完成发布；prod wxpay + notify（待商户证书）。移除 feat/job 的 dev auto-PUBLISHED（payment 接管发布）。PricingConfig seed（D30=90/D90=180）。小程序支付页。

### 新增文件
- `apps/server/src/modules/payment/payment.service.ts` - createJobPublishOrder（计价+下单+dev mock 发布）/ fulfillOrder（幂等置 PAID+PUBLISHED+expireAt）/ notify（回调验签）/ mockPay（dev）/ getOrder
- `apps/server/src/modules/payment/payment.controller.ts` - `POST /payments/job-publish`、`POST /payments/notify`(@Public)、`POST /payments/mock-pay/:orderId`(@Public dev)、`GET /payments/:orderId`
- `apps/server/src/modules/payment/payment.module.ts`
- `apps/server/src/modules/payment/dto/payment.dto.ts` - PublishJobDto（jobPostId+duration，无 amount）
- `apps/user-miniprogram/services/payment.ts` - publishJob
- `apps/user-miniprogram/pages/payment/index.{ts,wxml,wxss,json}` - 支付页（确认支付 -> dev 直接发布成功）

### 修改文件
- `apps/server/src/app.module.ts` - 接入 PaymentModule
- `apps/server/src/modules/job/job.service.ts` - **移除 createPost dev auto-PUBLISHED**（创建 PENDING，由 payment 发布）；更新方法注释
- `apps/server/prisma/seed.ts` - 加 PricingConfig 种子（D30=90, D90=180）
- `apps/user-miniprogram/pages/job/post/index.{ts,wxml}` - 创建草稿后跳支付页；hint 改为"需付费发布"
- `apps/user-miniprogram/pages/job/manage/index.{ts,wxml,wxss}` - PENDING 岗位显示「付费发布」按钮 -> 支付页
- `apps/user-miniprogram/app.json` - 注册 pages/payment

### 完成的工作
- ✅ `POST /payments/job-publish`：校验 merchant 拥有岗位 + 岗位 PENDING -> 按 PricingConfig 计价（服务端算）-> 下单 -> dev mock 完成（PAID + PUBLISHED + expireAt）
- ✅ `fulfillOrder` 幂等：置 PaymentOrder.PAID + JobPost.PUBLISHED + expireAt(now+30/90 天)
- ✅ `POST /payments/notify`(@Public)：微信回调验签 -> fulfillOrder（prod；dev 按 orderId 直达，真实验签 TODO）
- ✅ `POST /payments/mock-pay/:orderId`(@Public dev only)：模拟支付完成
- ✅ 移除 createPost dev auto-PUBLISHED：发岗为 PENDING，由 payment 发布（dev 走 mock，prod 走 notify）
- ✅ PricingConfig seed（D30=90, D90=180，admin 可改）
- ✅ 小程序支付页 + job/post 跳转 + job/manage 发布按钮
- ✅ 空上下文子代理测试全过（17 接口 smoke a-q：发岗 PENDING -> 付费发布 PAID/PUBLISHED -> 列表可见 -> 可报名 / 重复付费 50002 / D90=180 / notify+mock-pay 幂等 / 401）
- typecheck（server + 小程序）双通过
- 错误码 5xxxx 支付段：50001 订单不存在 / 50002 已发布或已下架 / 50004 单价未配置

### 潜在隐患
1. **微信支付真实下单未实现**：prod 分支 TODO（V3 JSAPI 下单 + 回调验签需商户证书），当前 fallback mock；生产前必须补齐。
2. **notify 验签简化**：dev 按 orderId 直达，无签名校验；prod 必须实现 V3 回调验签 + 解密。
3. **dev mock 直接发布**：dev 模式跳过支付，直接 PUBLISHED；仅适合开发，prod 走真实支付。
4. **金额 Decimal**：返回 amount 为字符串（Decimal.toString），前端展示需注意。
5. **无退款**：PaymentOrder 有 REFUNDED 状态但无退款接口；P1 加（岗位审核驳回退款）。
6. **expireAt 由 payment 设**：createPost 设占位 expireAt，payment 发布时重设；若永不付费，占位 expireAt 无意义（PENDING 岗位不展示）。
7. **l 项偏差**：普通用户付费被 60002（非商家）先拦，非 10003；安全性达标（用例设计偏差，非缺陷）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. **真实微信支付**：配商户证书 + 实现 V3 JSAPI 下单 + notify 验签解密。
3. 退款接口（feat/admin 审核驳回 -> 退款）。
4. `feat/admin`：PricingConfig 单价配置页。
5. `feat/cron-deploy`：到期下架 cron。

---

## 2026-07-10 · feat/job 兼职岗位/报名/评价

**发生时间**：2026-07-10 23:04 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过，含完整交易闭环）
**commit**：未提交（待 squash 合并 feat/job）

### 改动概要
兼职核心闭环：商家发岗（dev auto-PUBLISHED + 内容安全）/ 列表游标分页 / 详情 / 用户报名（防重）/ 状态流转 accept-complete（非法 40004）/ 评价（1 次）/ 岗位评价列表。商家发岗需 Merchant APPROVED。小程序用户（列表/详情/报名/我的报名/评价）+ 商家（发岗/管理报名）5 页。

### 新增文件
- `apps/server/src/modules/job/job.service.ts` - createPost/listPosts/getPost/apply/listApplications/listMyApplications/transition/review/listReviews
- `apps/server/src/modules/job/job.controller.ts` - `POST /job-posts`、`GET /job-posts`、`GET /job-posts/:id`、`POST /job-posts/:id/applications`、`GET /job-posts/:id/applications`、`GET /job-posts/:id/reviews`、`GET /applications/me`、`POST /applications/:id/transition`、`POST /applications/:id/review`
- `apps/server/src/modules/job/job.module.ts`
- `apps/server/src/modules/job/dto/job.dto.ts` - CreateJobPostDto/JobListQueryDto/TransitionDto/CreateReviewDto
- `apps/server/prisma/migrations/20260710225321_job_app_user/migration.sql` - JobApplication 加 user 外键
- `apps/user-miniprogram/services/job.ts` - 接口封装
- `apps/user-miniprogram/pages/job/{detail,post,my-applications,manage}/index.{ts,wxml,wxss,json}` - 4 新页
- `apps/user-miniprogram/pages/job/index.{wxss,json}` - 列表页配置（ts/wxml 重写）

### 修改文件
- `apps/server/prisma/schema.prisma` - JobApplication 加 `user User @relation`；User 加 `applications JobApplication[]`
- `apps/server/src/app.module.ts` - 接入 JobModule
- `apps/user-miniprogram/pages/job/index.{ts,wxml}` - 占位页改为岗位列表（用户浏览 + 商家入口）
- `apps/user-miniprogram/app.json` - 注册 job/detail、job/post、job/my-applications、job/manage

### 完成的工作
- ✅ 商家发岗：校验 Merchant APPROVED + `checkText(description)`；dev auto-PUBLISHED + expireAt（生产等 payment 回调）
- ✅ 岗位列表：PUBLISHED 且未过期，游标分页；`mine=1` 商家自己的（含草稿）
- ✅ 用户报名：`@@unique([jobPostId,userId])` 防重（P2002 -> 40002）；岗位须 PUBLISHED 未过期
- ✅ 状态流转：accept(PENDING->ACCEPTED，商家) / complete(ACCEPTED->DONE，商家或学生)；非法流转 40004
- ✅ 评价：仅学生 + DONE + 未评过（P2002 -> 40005）；未完成 40005
- ✅ 岗位评价列表
- ✅ 权限：非商家发岗 60003；非拥有者查报名 403/10003；无 token 401
- ✅ 小程序 5 页：列表(用户+商家入口) / 详情(用户报名+商家管理报名双模式) / 发岗 / 我的报名(含评价) / 管理岗位
- ✅ 空上下文子代理测试全过（21 接口 smoke a-u + 静态一致性）
- typecheck（server + 小程序）双通过
- 注：状态流转用 `POST /applications/:id/transition`（wx.request 不支持 PATCH，偏离 API 规范 §6.4 PATCH）

### 潜在隐患
1. **dev auto-PUBLISHED**：发岗自动发布，生产需 feat/payment 回调置 PUBLISHED + 扣费。
2. **无 PATCH**：transition 用 POST /transition，偏离 API 规范；admin-web 未来可用 PATCH。
3. **CANCELLED 未实现**：状态机无取消（学生取消报名 / 商家拒绝），P1 加。
4. **expireAt 未自动置 EXPIRED**：列表过滤 expireAt>now，但 status 不变；feat/cron-deploy 定时扫。
5. **评价无商家回复**：仅学生评价，商家回复 P1。
6. **岗位列表无搜索/筛选**：MVP 仅时间倒序；P1 加关键词/地点筛选。
7. **报名无通知**：商家/学生无实时通知（P1 站内通知）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. `feat/payment`：付费发布（回调置 PUBLISHED + expireAt + 扣费）。
3. `feat/cron-deploy`：到期下架 cron。
4. CANCELLED 状态 + 通知。
5. 岗位搜索/筛选。

---

## 2026-07-10 · feat/merchant 商家入驻

**发生时间**：2026-07-10 22:29 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/merchant）

### 改动概要
商家入驻：register（dev 自动审核通过 + 加 MERCHANT 角色，生产等 admin）/ profile 查询 / PUT 更新；**收紧 auth ensureRole merchant 需 Merchant APPROVED**（未入驻 60003）；小程序入驻表单/资料页 + profile 入口。

### 新增文件
- `apps/server/src/modules/merchant/merchant.service.ts` - register/getProfile/updateProfile + `approveInternal`/`rejectInternal` helper（供 feat/admin）
- `apps/server/src/modules/merchant/merchant.controller.ts` - `POST /merchant/register`、`GET /merchant/profile`、`PUT /merchant/profile`
- `apps/server/src/modules/merchant/merchant.module.ts`
- `apps/server/src/modules/merchant/dto/{register-merchant,update-merchant}.dto.ts`
- `apps/user-miniprogram/services/merchant.ts` - registerMerchant/getMerchantProfile/updateMerchantProfile
- `apps/user-miniprogram/pages/merchant/register/index.{ts,wxml,wxss,json}` - 入驻表单
- `apps/user-miniprogram/pages/merchant/profile/index.{ts,wxml,wxss,json}` - 资料展示 + 编辑

### 修改文件
- `apps/server/src/app.module.ts` - 接入 MerchantModule
- `apps/server/src/modules/auth/auth.service.ts` - **ensureRole merchant 收紧**：查 Merchant APPROVED 否则 60003（原宽松自动加改为需审核通过）；import MerchantStatus
- `apps/user-miniprogram/services/request.ts` - method 类型调整（wx.request 不支持 PATCH，updateProfile 用 PUT）
- `apps/user-miniprogram/app.json` - 注册 merchant/register + merchant/profile 页
- `apps/user-miniprogram/pages/profile/index.{ts,wxml}` - 加 `goMerchant` 入口

### 完成的工作
- ✅ `POST /merchant/register`：创建 Merchant(PENDING)，dev 自动 APPROVED + 加 MERCHANT 角色；生产等 admin
- ✅ `GET /merchant/profile`：查当前用户 Merchant
- ✅ `PUT /merchant/profile`：改 shopName/contactPhone（wx.request 不支持 PATCH，用 PUT）
- ✅ **auth ensureRole merchant 收紧**：未入驻/未审核 60003（子代理验：未入驻 merchant 登录 403/60003；入驻后 merchant 登录 role=MERCHANT）
- ✅ `approveInternal`/`rejectInternal` helper：供 feat/admin（审核通过加 MERCHANT 角色，拒绝移除）
- ✅ 小程序入驻表单（shopName/licenseNo/contactPhone 校验）+ 资料展示编辑
- ✅ profile 页「商家入驻/资料」入口
- ✅ 空上下文子代理测试全过（11 接口 smoke + 静态一致性）
- typecheck（server + 小程序）双通过
- 新增错误码 6xxxx 商家段：60001 已入驻 / 60002 未入驻 / 60003 未审核通过

### 潜在隐患
1. **dev auto-approve**：register 自动 APPROVED + 加 MERCHANT 角色，生产需 feat/admin 手动审核（approveInternal 已就绪）。
2. **licenseNo 未校验真伪**：只校验非空长度，未调工商接口验真；MVP 可接受。
3. **营业执照图片上传未做**：register 仅文字字段，资质图片未上传；后续可加 /uploads 集成。
4. **contactPhone 可改但无验证码**：改电话无短信验证；MVP 可接受。
5. **收紧影响 auth-role 测试**：feat/auth-role 的 merchant 宽松用例（无 Merchant 直接 merchant 登录）现在 60003，是预期收紧。
6. **wx.request 无 PATCH**：updateProfile 用 PUT，DTO optional 部分更新，语义可接受。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. `feat/admin` 用 approveInternal/rejectInternal 实现商家审核。
3. 资质图片上传（/uploads 集成）。
4. `feat/job` 商家发岗前校验 Merchant APPROVED。

---

## 2026-07-10 · feat/chat-real 自建 WebSocket 真实接入 + room 原语

**发生时间**：2026-07-10 21:28 +0800
**状态**：已审查（typecheck + 空上下文子代理 ws 端到端测试全过，含 1v1/房间广播/心跳/鉴权）
**commit**：未提交（待 squash 合并 feat/chat-real）

### 改动概要
自建 WebSocket 真实接入（替代 MobileIMSDK--其小程序端闭源需捐助、服务端 Java+RabbitMQ 过重）。`ChatGateway`（ws.Server + 连接鉴权 / 1v1 转发 / 房间原语 / 心跳）+ `ImService` 改签 JWT(type=ws) + 小程序 `im.ts`（wx.connectSocket + login + 1v1/房间 + 心跳 + 断线重连）。覆盖客服 1v1 / 岗位 1v1 / 树洞 1v1 + 多人派对房。

### 新增文件
- `apps/server/src/modules/chat/chat.gateway.ts` - ws.Server 网关：login JWT 校验、msg 1v1 转发+回执、join/leave/room-msg 房间广播（排除发送方）、room_event 成员进出通知、ping/pong 心跳 + 30s 超时踢、断线自动移出房间

### 修改文件
- `apps/server/src/modules/chat/im.service.ts` - 改自建：`JwtService` 签 `loginToken`(type=ws, 1h) + `wsUrl`，去 mock 分支
- `apps/server/src/modules/chat/chat.module.ts` - providers 加 `ChatGateway`
- `apps/server/.env` + `.env.example` - `MOBILEIMSDK_*` 换 `CHAT_WS_URL`(ws://localhost:3001) / `CHAT_WS_PORT`(3001)
- `apps/user-miniprogram/services/im.ts` - 重写：`wx.connectSocket` 直连 + login + msg/join/leave/room-msg + 20s 心跳 + 3s 断线重连 + onMessage/onRoomMessage/onWsEvent 回调
- `apps/server/package.json` + `pnpm-lock.yaml` - 加 `ws` ^8.21.0 + `@types/ws`

### 完成的工作
- ✅ `ChatGateway` ws.Server（端口 3001）：连接鉴权（JWT type=ws 校验 identifier = uid 或 anonId）
- ✅ 1v1 消息实时转发（msg -> toId 在线连接 + 发送方 msg_sent 回执）
- ✅ 房间原语：join/leave/room-msg 广播（`broadcastRoom` 排除发送方，不回发自己）；room_event 通知成员进出
- ✅ 心跳：ping/pong + 30s 超时踢；断线自动从所有房间移除并广播
- ✅ `ImService` 改签 JWT loginToken（1h），wsUrl 配置化
- ✅ 小程序 `im.ts`：wx.connectSocket + login + 1v1/房间 + 20s 心跳 + 3s 断线重连
- ✅ 空上下文子代理 ws 端到端测试全过（13/13）：login / 1v1 / join / room-msg(exclude 修复) / leave / 未加入房间不能发 / ping
- typecheck（server + 小程序）双通过
- 修复：`handleRoomMsg` 漏传 exclude（发送方收到自己 room-msg）-> 子代理重测验证无回归

### 潜在隐患
1. **离线消息丢弃**：ws 实时只转发在线连接，离线不存；靠 HTTP 历史接口拉取（`/chat/messages` 落库）。客户端上线需主动拉历史。
2. **ws 单实例**：ChatGateway 内存维护连接/房间，多实例部署需共享（Redis pub/sub 或 sticky session）；MVP 单实例够用。
3. **房间无持久化**：派对房为临时房间，服务重启丢失；树洞派对房生命周期由 feat/treehole 管理。
4. **心跳间隔**：30s 检查 + 客户端 20s ping，弱网可能误踢；可调。
5. **未读消息数**：ChatSession 未存 unreadCount，后续加。
6. **wsUrl 生产 wss**：生产需 wss://域名 + TLS（nginx 反代 ws）。
7. **多端登录**：conns 是 `Map<id, Set<ws>>` 支持多端，同端多登录消息全转发可能重复；MVP 可接受。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. `feat/treehole` 用 `ImService`(anonId) + ChatGateway room 建匿名派对房 + 1v1 匹配。
3. `feat/job` 站内沟通用 chat（uid 1v1）。
4. 客服 IM（用户 1v1 客服 uid）。
5. `ChatSession` 加 `unreadCount`。
6. 生产 wss + nginx ws 反代配置。

---

## 2026-07-10 · feat/chat 聊天业务层 + ImService 适配层

**发生时间**：2026-07-10 18:11 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/chat）

### 改动概要
chat 模块：`ChatMessage`/`ChatSession` 模型 + 迁移；`ImService` 适配层（mock/真实 MobileIMSDK，真实接入 TODO）；`ChatService` 消息/会话 CRUD（双向会话、游标分页）；4 个接口；小程序 `im.ts` 骨架。真实 MobileIMSDK 服务端 + SDK 接入待获取 SDK 代码后补，接口契约已定，treehole/job 可消费。

### 新增文件
- `apps/server/src/modules/chat/im.service.ts` - ImService 适配层（`getImCredential(identifier)`，mock 返回 mock 凭证，真实分支 TODO）
- `apps/server/src/modules/chat/chat.service.ts` - 消息/会话 CRUD（双向会话 upsert、游标分页、双向 OR 查询）
- `apps/server/src/modules/chat/chat.controller.ts` - `POST /chat/token`、`POST /chat/messages`、`GET /chat/messages`、`GET /chat/sessions`
- `apps/server/src/modules/chat/chat.module.ts` - `@Global()`，exports `ImService`/`ChatService`
- `apps/server/src/modules/chat/dto/{send-message,chat-query}.dto.ts`
- `apps/server/prisma/migrations/20260710175207_chat_messages/migration.sql`
- `apps/user-miniprogram/services/im.ts` - im 管理器骨架（connectIm/sendIm/listMessages/listSessions/getImToken/onMessage）

### 修改文件
- `apps/server/prisma/schema.prisma` - 新增 `ChatMessage`（fromId/toId/content，双向索引）、`ChatSession`（ownerId+peerId 唯一）
- `apps/server/src/app.module.ts` - 接入 ChatModule
- `apps/server/.env` + `.env.example` - 加 `MOBILEIMSDK_WS_URL`/`MOBILEIMSDK_API_URL`/`MOBILEIMSDK_SECRET`

### 完成的工作
- ✅ `ChatMessage`/`ChatSession` 模型 + 迁移（双向消息索引 `[from,to,createdAt]`/`[to,createdAt]`、会话唯一约束 `[owner,peer]`）
- ✅ `ImService` 适配层：`getImCredential(identifier)`，mock 模式返回 mock 凭证（`ws://mock-mobileimsdk`），真实接入 TODO
- ✅ `ChatService`：`sendMessage`（双向 ChatSession upsert）、`listMessages`（游标分页、双向 OR）、`listSessions`
- ✅ 4 接口：`POST /chat/token`、`POST /chat/messages`、`GET /chat/messages`、`GET /chat/sessions`
- ✅ 小程序 `im.ts` 骨架（connectIm/sendIm/历史拉取，真实 SDK 接入 TODO）
- ✅ 空上下文子代理测试全过：token 凭证 / 消息收发 / 双向历史 / 双向会话 / 分页翻页 / 401 + DB 表结构 + 静态一致性
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **MobileIMSDK 真实接入未完成**：`ImService.getImCredential` 真实分支仍回退 mock（TODO），需获取 MobileIMSDK Node 服务端 SDK/文档后实现（注册账号 + 签发 loginToken）；小程序 `im.ts` connect 用原生 `wx.connectSocket` 占位，缺 QoS/心跳/重连，需换 MobileIMSDK 小程序端 SDK。
2. **消息实时传输未实现**：当前消息只 HTTP 落库，无 WebSocket 实时推送；真实接入后由 MobileIMSDK 服务端承担实时，NestJS 侧只持久化。
3. **mock 模式无真实聊天**：`/chat/token` 返回 mock wsUrl，小程序 connectIm 跳过 socket；只能测 HTTP 消息接口，不能测 1v1 实时收发。
4. **分页排序**：全量列表 old->new，分页翻页 new->old（每页内部 old->new），子代理观察到方向差异（非 bug，翻页语义）。
5. **未读消息数**：ChatSession 未存 unreadCount，后续需加。
6. **匿名隔离**：ChatMessage fromId/toId 用标识符（uid 或 anonId），无 FK；treehole 用 anonId 时需自行保证不落 uid（feat/treehole 责任）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 + push。
2. **真实 MobileIMSDK 接入**：获取 Node 服务端代码（git clone / npm）+ 小程序端 SDK JS 文件，实现 `ImService` 真实签发 + `im.ts` 用 SDK `loginImpl`/`sendData`。
3. `ChatSession` 加 `unreadCount`。
4. `feat/treehole` 用 `ImService`（anonId）+ `ChatService` 实现匿名 1v1 聊。
5. `feat/job` 站内沟通用 chat（uid）。

---

## 2026-07-10 · feat/auth-role 多角色登录改造

**发生时间**：2026-07-10 17:37 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke 全过）
**commit**：未提交（待 squash 合并 feat/auth-role）

### 改动概要
多角色登录改造：一个微信账号多角色并存（`UserRole` 表）+ 静默切换角色 + 管理员 openid 预设绑定。后端 + 小程序角色选择页 + 切换/退出。三端合一后的登录地基。

### 新增文件
- `apps/server/src/modules/auth/admin.guard.ts` - 管理员守卫（`req.user.role === 'ADMIN'`，否则 403/10003）
- `apps/server/src/modules/auth/dto/switch-role.dto.ts` - `{ role: user|merchant|admin }`
- `apps/user-miniprogram/pages/role-select/index.{ts,wxml,wxss,json}` - 角色选择页（用户/商家/管理员三卡）
- `apps/server/prisma/migrations/20260710172428_auth_role_multirole/migration.sql` - 建 Role enum + user_roles 表 + admin_users.openid + 删 users.role

### 修改文件
- `apps/server/prisma/schema.prisma` - 去 `User.role`；enum `UserRole`->`Role`；新增 `UserRole` 表（userId+role 唯一）；`AdminUser` +`openid`（唯一）、`passwordHash` 改可选
- `apps/server/src/modules/auth/auth.service.ts` - wx-login 校验拥有角色 + admin openid 绑定；`switchRole` 静默切换；`issueTokens` 用传入 role；`toUserVo` 返回 `roles` 数组；code2session 统一用 WX_USER_APPID/SECRET（三端合一单小程序）
- `apps/server/src/modules/auth/auth.controller.ts` - 加 `POST /auth/switch-role`（需 Bearer）
- `apps/server/src/modules/auth/auth.module.ts` - provide/export `AdminGuard`
- `apps/server/src/modules/auth/dto/wx-login.dto.ts` - role 加 `admin`
- `apps/server/prisma/seed.ts` - `AdminUser` 种子（`mock_admin` openid，可由 `ADMIN_OPENID` 环境变量覆盖）
- `apps/user-miniprogram/app.ts` - `loginWithRole`/`switchRole`/`logout`/`requireAuth` + `globalData.currentRole`
- `apps/user-miniprogram/utils/auth.ts` - `loginWithRole`/`switchRole`/`restoreAuth`/`clearAuth`/`formatTime`
- `apps/user-miniprogram/app.json` - 注册 `pages/role-select/index`
- `apps/user-miniprogram/pages/confession/index.ts`、`post-create/index.ts`、`post-detail/index.ts` - onShow/onLoad 改 `requireAuth()`（去 `waitLogin`）
- `apps/user-miniprogram/pages/profile/index.{ts,wxml,wxss}` - 当前角色徽标 + 切换角色列表 + 退出登录

### 完成的工作
- ✅ 多角色并存：`UserRole` 关联表，同用户可同时拥有 USER+MERCHANT+ADMIN（子代理验证 roles=[USER,MERCHANT]）
- ✅ 角色选择登录：小程序角色选择页 -> `wx.login` -> `wx-login(role)`
- ✅ 静默切换角色：`POST /auth/switch-role`（Bearer），不需重新 wx.login，子代理验证 user<->merchant 切换
- ✅ 管理员 openid 预设绑定：`AdminUser.openid`，未绑定 `10003`（code=admin 成功、code=notadmin 403）
- ✅ `AdminGuard`（role===ADMIN），供 feat/admin 用
- ✅ 去掉 `User.role` 单值，当前角色由 `token.role` 携带
- ✅ 空上下文子代理测试全过：10 接口 smoke + DB 状态（user_roles 建表 / users.role 删除 / admin_users.openid）+ 静态一致性
- typecheck（server + 小程序）双通过

### 潜在隐患
1. **merchant 角色当前宽松**：wx-login/switch-role 自动加 MERCHANT，生产需 feat/merchant 收紧（需 Merchant APPROVED 才加）。
2. **切换角色旧 token 不失效**：2h 自然过期，如需严格加 Redis 黑名单。
3. **管理员 openid 开发期 mock**：种子 `mock_admin`，上线前需用 `ADMIN_OPENID` 环境变量换真实 openid。
4. **小程序管理员按钮 mock 模式失效**：真实 wx.login code 是随机值，mock openid 不匹配种子；mock 测管理员只能走 curl `code=admin`。需配真实 WX 凭证才能在小程序里测管理员。
5. **无 admin 接口实际挂 @AdminGuard**：feat/admin 才会用；AdminGuard 逻辑已由 switch-role admin 拒绝间接验证。
6. **去 User.role 破坏性迁移**：现有 users.role 数据被删，开发期可接受（重新登录加 USER 到 UserRole）。

### 后续待做工作 / 方向
1. 提交 + squash 合并 main + push。
2. `feat/merchant` 收紧 merchant 角色获得（Merchant APPROVED 才加 MERCHANT）。
3. `feat/admin` 用 `@AdminGuard` 保护 admin 接口。
4. 上线前 `ADMIN_OPENID` 换真实管理员 openid。
5. W0 下一个：`feat/chat`（MobileIMSDK 接入）。

---

## 2026-07-10 · 修正：单角色 -> 多角色 + 静默切换

**发生时间**：2026-07-10 16:39 +0800
**状态**：待检查（文档同步，无代码运行时测试）
**commit**：未提交

### 改动概要
修正上一条「IM 换 MobileIMSDK + 三端合一」中的角色设计：原「单角色 + 退出重选」改为「**多角色并存 + 静默切换**」，因开发时需多角色切换、且一个微信账号可同时是用户/商家/管理员。

### 修改文件
- `docs/开发记录/分支开发计划.md` - 0.1 变更点 5-6 改为多角色 + 静默切换；`feat/auth-role` brief 数据模型/暴露契约/完成标准/隐患相应调整（新增 `UserRole` 表、去掉 `User.role`、`POST /auth/switch-role` 静默切换）；§4 协同契约、§5 prisma 协议同步

### 飞书文档同步
- 《需求变更声明-20260709》overwrite（变更三登录流程 + 变更四数据模型改为多角色 + UserRole 表 + 去掉 User.role）

### 完成的工作
- ✅ 多角色方案落地：`UserRole` 关联表（userId + role 唯一）存角色集合，当前角色由 `token.role` 携带
- ✅ 静默切换方案：前端「切换角色」-> 静默 `wx.login` -> `wx-login(newRole)` -> 新 token，不退出微信
- ✅ 角色获得规则：用户默认有；商家 Merchant APPROVED 时加；管理员 AdminUser.openid 绑定时加
- ✅ 飞书变更声明文档同步

### 潜在隐患
1. **去掉 `User.role` 影响现有 auth 代码**：`auth.service.issueTokens` 当前用 `user.role` 签 token，`feat/auth-role` 须改为用登录传入的 role；confession 等模块未直接用 `User.role`，影响面小。
2. **切换角色旧 token 不失效**：静默切换后旧 token 仍有效至过期（2h），短期可接受；如需严格可加 Redis 黑名单。
3. **UserRole 迁移**：新增表 + 去掉 User.role 需迁移；现有 users 数据的 role 要回填到 UserRole（或清表重来，开发期可接受）。
4. **角色选择页逻辑**：要展示「该用户拥有的角色」，未拥有的角色（如管理员）不展示或置灰，避免选了被拒的体验。

### 后续待做工作 / 方向
1. 提交本次文档变更（分支计划 + project.config + 两条改动记录）到 git。
2. 开始 `feat/auth-role`：新增 UserRole 表 + 迁移、改 auth.service、角色选择页、静默切换。
3. 用户提供管理员真实 openid（或先用 mock openid 占位）。

---

## 2026-07-10 · 需求变更：IM 换 MobileIMSDK + 三端合一（同步开发计划）

**发生时间**：2026-07-10 16:26 +0800
**状态**：待检查（文档同步，无代码运行时测试）
**commit**：未提交

### 改动概要
需求变更同步到开发计划文档：IM 由腾讯云 TIM 换 **MobileIMSDK**（开源 Apache-2.0，Node 服务端 + 小程序端原生 SDK）；三端合一（取消独立商家端小程序 / admin-web，合一个单小程序角色选择）；登录流程改造（角色选择 -> 微信登录，管理员 openid 预设绑定，退出重选，单角色）。小程序 appid 配为 `wx6d60329e0114a09a`。

### 修改文件
- `docs/开发记录/分支开发计划.md` - 整体重写：新增 `feat/auth-role`（角色登录改造，W0 最先）、`feat/chat` 改 MobileIMSDK 接入、`feat/admin` 改小程序内管理员角色（取消 admin-web）、取消 `merchant-miniprogram`；DAG / 协同契约 / 共享文件协议相应调整
- `apps/user-miniprogram/project.config.json` - appid 改 `wx6d60329e0114a09a`，description / projectname 更新（三端合一）

### 飞书文档同步
- 《开发路线图》overwrite 整篇（TIM->MobileIMSDK + 三端合一 + 各 Phase 调整）
- 《小程序功能模块设计方案》append 变更声明（成功）
- 新建《需求变更声明-20260709》文档（集中说明变更 + 影响清单）
- 《技术栈》《API设计规范》《数据模型设计》append 失败（lark-cli block 末尾定位 bug `target block not found`），以《需求变更声明-20260709》为准

### 完成的工作
- ✅ IM 方案替换落地到计划：MobileIMSDK（Node 服务端独立容器 + 小程序端 SDK + NestJS chat 业务层）
- ✅ 三端合一落地：单小程序角色选择，取消 admin-web / 独立商家端
- ✅ 登录流程改造方案：角色选择 -> `wx-login(role)` -> 管理员 openid 预设绑定 -> 退出重选 -> 单角色
- ✅ 小程序 appid 配置真实 appid `wx6d60329e0114a09a`
- ✅ 飞书路线图 overwrite + 新建变更声明文档

### 潜在隐患
1. **3 份飞书文档 append 失败**：《技术栈》《API设计规范》《数据模型设计》原文 IM / 三端相关内容未直接改，靠《需求变更声明-20260709》指向；后续应 overwrite 这 3 份统一更新。
2. **MobileIMSDK 服务端独立部署**：多一个容器，运维成本增加；消息存储双份（MobileIMSDK 自存 + NestJS 业务侧）需对账。
3. **管理员 openid 种子**：预设绑定需用户提供真实管理员 openid；当前无真实 openid，`feat/auth-role` 开发时先用 mock openid。
4. **三端合一路由**：单小程序页面数增多，角色路由要严格（角色不符的页面不可进）。
5. **小程序 WebSocket**：生产需备案域名 + TLS 证书，测试关域名校验。

### 后续待做工作 / 方向
1. 手动 overwrite 飞书《技术栈》《API设计规范》《数据模型设计》3 份（或后续迭代统一更新）。
2. 提交本次文档变更（分支计划 + project.config + 改动记录）到 git。
3. 开始 `feat/auth-role`（角色选择登录改造，W0 最先）。
4. 开始 `feat/chat`（MobileIMSDK 接入）。
5. 用户提供管理员真实 openid 写入 `AdminUser` 种子。

---

## 2026-07-09 · 新增 AGENTS.md 项目级 agent 行为约束

**发生时间**：2026-07-09 21:57 +0800
**状态**：待检查（纯文档，无运行时测试需求）
**commit**：未提交

### 改动概要
新增项目根 `AGENTS.md`，固化所有 agent（主代理 + 子代理）的强制行为约束。直接起因：2026-07-06 测试子代理测完自作主张 `docker compose down` 删了 postgres 容器（所幸 named volume 保住数据），需把基础设施红线写死，防再犯。

### 新增文件
- `AGENTS.md` - 10 节：项目速览 / 工作流入口（先 /yitong-dev）/ 🔴基础设施保护红线（禁 docker compose down、禁删 volume、禁改 .env、禁改写已推送历史）/ 改动记录约束 / 测试门槛（空上下文子代理，不得自证）/ 提交合并（trunk-based squash）/ 文档落点 / 技术红线（匿名 / 支付 / 内容安全）/ 常见坑速查 / 分支协同 / 子代理特别约束
- `CLAUDE.md` - 一行 `@AGENTS.md` 导入 + 核心要求摘要；Claude Code 自动加载 CLAUDE.md 时把 AGENTS.md 全文导入，约束自动生效（单一事实源在 AGENTS.md，不重复）

### 完成的工作
- ✅ 基础设施红线成文：`docker compose down` 永久禁止（只能 `stop`）；`down -v` / `volume rm` / `system prune` 禁止
- ✅ 子代理特别约束：默认只读+测试，不碰 docker/git/.env，不确定就停下问
- ✅ 与 `yitong-dev` skill 互补定位：skill = 详细工作流步骤，AGENTS.md = 规则红线 + 项目坑速查
- ✅ 覆盖提交规范、文档落点、技术红线、分支协同
- ✅ 建 `CLAUDE.md` 用 `@AGENTS.md` 导入语法，让约束在项目内自动加载生效（单一事实源，不重复不漂移）

### 潜在隐患
1. ✅ ~~不会自动加载~~ 已解决：建了 `CLAUDE.md` 用 `@AGENTS.md` 导入，Claude Code 在项目内自动加载时约束生效。注：**子代理不自动加载 CLAUDE.md**，仍需主代理派发时在 prompt 里要求「先读项目根 AGENTS.md」。
2. **无强制机制**：约束靠 agent 自觉读 + 主代理 prompt 传达；子代理若未被告知仍可能违规（需主代理每次派发都带约束）。
3. **与 skill 内容部分重叠**：AGENTS.md 与 yitong-dev skill 在红线/坑上有重复；后续 skill 更新需同步本文件，否则漂移。

### 后续待做工作 / 方向
1. ✅ ~~建 `CLAUDE.md`~~ 已完成（`@AGENTS.md` 导入，自动加载生效）。
2. **提交**：`AGENTS.md` + `docs/开发记录/分支开发计划.md`（均 untracked）一起提交。
3. **主代理派发子代理模板**：固化 prompt 开头「先读项目根 AGENTS.md」一句话。
4. 后续 yitong-dev skill 有变更时同步 AGENTS.md 对应章节。

---

## 2026-07-06 · Phase 1.2 表白墙前端（小程序登录态 + 圈子/发帖/详情/评论）

**发生时间**：2026-07-06 21:41 +0800
**状态**：已审查（typecheck + 空上下文子代理 smoke + 静态一致性 + VO 对齐全过；未在微信开发者工具实机渲染）
**commit**：未提交（待开 feat/mp-confession 分支）

### 改动概要
用户端小程序登录态接入 + 表白墙前端四页落地：app 启动静默 wx.login 换 token（storage + globalData）；首页横滑圈子 Tab + 发现流（游标分页 + 下拉刷新 + 上拉加载更多）；帖子详情（正文/图片九宫格/评论列表/底部发评栏）；发帖页（圈子选择弹窗 + 文字 + 图片选择+上传 COS + 发布）；悬浮发帖 FAB；帖子卡片组件；profile 页展示头像昵称。

### 新增文件
- `apps/user-miniprogram/utils/auth.ts` — 静默登录 / storage / me 拉取 / 相对时间格式化 / clearAuth
- `apps/user-miniprogram/services/confession.ts` — 表白墙接口封装（listCircles/feed/createPost/toggleLike/createComment/listComments 等类型对齐后端 PostVo/CommentVo/FeedResult）
- `apps/user-miniprogram/services/upload.ts` — wx.uploadFile 到 `/uploads`，顺序批量上传 uploadImages
- `apps/user-miniprogram/components/post-card/post-card.{ts,wxml,wxss,json}` — 帖子卡片组件（头像占位/昵称/时间/正文/九宫格图/点赞/评论），like/comment 用 catchtap 阻止冒泡
- `apps/user-miniprogram/pages/post-detail/index.{ts,wxml,wxss,json}` — 帖子详情（正文渲染 + 评论分页加载 + 底部输入栏 + 点赞/发送）
- `apps/user-miniprogram/pages/post-create/index.{ts,wxml,wxss,json}` — 发帖页（圈子选择 bottom-sheet、textarea、九宫格选图+删除+预览、图片上传后 createPost）
- `apps/user-miniprogram/pages/confession/index.json` — 页面配置（usingComponents post-card、enablePullDownRefresh）
- `apps/user-miniprogram/pages/profile/index.json`
- `apps/user-miniprogram/pages/treehole/index.json`、`pages/job/index.json` - 占位页补最小 json（子代理测试建议，保持一致性）
- `apps/user-miniprogram/tsconfig.json` — 小程序 TS typecheck 配置（strict、skipLibCheck、types=[miniprogram-api-typings]）

### 修改文件
- `apps/user-miniprogram/app.ts` — 重写：onLaunch 静默 ensureLogin、waitLogin() 等待队列、globalData(token/refreshToken/user/loginReady)、AppInstance 类型导出
- `apps/user-miniprogram/app.json` — selectedColor 改为品牌红 `#ff4d6d`；注册 post-detail/post-create 页面
- `apps/user-miniprogram/app.wxss` — 引入字体栈、去 button 默认边框
- `apps/user-miniprogram/pages/confession/index.{ts,wxml,wxss}` — 完整重写（横滑圈子 Tab、发现/圈子内容流、cursor 分页、下拉刷新、上拉加载、乐观点赞更新、悬浮 FAB）
- `apps/user-miniprogram/pages/profile/index.{ts,wxml,wxss}` — 接入 waitLogin 展示头像/昵称；菜单项（我的表白墙/树洞/兼职，占位 toast 开发中）
- `apps/user-miniprogram/pages/job/index.{ts,wxml}`、`pages/treehole/index.{ts,wxml}` — 保留占位未动（tabBar 不报错）
- `apps/user-miniprogram/services/request.ts` — 扩展 method 支持所有合法 HTTP method（配合小程序 types）
- 根 `package.json` / `pnpm-lock.yaml` — 加 devDep `miniprogram-api-typings ^5.2.1`（workspace-root）

### 完成的工作
- ✅ 启动静默登录：wx.login → `/auth/wx-login`（mock 走 mock_openid，真凭证模式兼容）→ token 存 storage + globalData → fetchMe 更新昵称
- ✅ `waitLogin()` Promise 等待队列：页面 onShow 里 `await app.waitLogin()` 保证接口发出时 token 已就绪
- ✅ 表白墙首页：横滑圈子 Tab + 「发现」默认，选中切换内容；feed/listCirclePosts 用 cursor 分页；下拉刷新/上拉加载
- ✅ 帖子详情：正文、九宫格图片（one/two/three 三档布局）、wx.previewImage 预览、点赞 toggle、评论分页加载、底部输入栏（focus 参数支持从列表直接跳来聚焦）
- ✅ 发帖：圈子弹窗选择（默认选「表白」圈，若存在）、textarea（maxlength 2000 auto-height）、chooseMedia 选图（≤9）、删除/预览、按顺序 uploadImages 到 COS → createPost，成功后 navigateBack 并触发上一页 onShow 刷新
- ✅ 点赞乐观更新：UI 立即改，请求失败回滚
- ✅ 品牌视觉：主色 `#ff4d6d`（粉色）、卡片圆角 16rpx、悬浮 FAB 带阴影、头像占位用渐变圆+首字（无需引入默认图资源）
- ✅ 后端无改动；前端 tsc `tsc --noEmit` 通过（EXIT=0，依赖 miniprogram-api-typings）
- ✅ 空上下文子代理独立测试通过：后端 8 接口 smoke + 小程序 typecheck + wxml/方法绑定/组件引用/import/globalData/upload 用法静态一致性 + 后端 VO 与前端类型完全对齐

### 潜在隐患
1. **未在微信开发者工具真机/模拟器跑过**：仅命令行 typecheck 通过，WXML/WXSS 渲染、wx.* API 实际行为、tabBar/navigateTo 跳转需要在微信开发者工具里验证。
2. ✅ ~~未做端到端 smoke~~ 已做：空上下文子代理跑通后端 8 接口 + 小程序 typecheck + 静态一致性。注：未在微信开发者工具实机渲染（子代理能力受限，需人工验证 WXML/WXSS 渲染与 wx.* API 实际行为）。
3. **图片上传顺序串行**：`uploadImages` for await 一张一张传，9 张图会慢；后续可改 Promise.all 并发 + loading。
4. **乐观点赞无节流**：快速连点会发多次 toggleLike，后端 PostLike 唯一约束会兜底，但前端可加 500ms 节流。
5. **登录失败静默**：waitLogin reject 后页面直接 return，没给「重试登录」按钮；离线/网络失败体验较差。
6. **storage 没过期处理**：token 过期（401）时仅 utils/auth.fetchMe 里 clearAuth，request 封装没在 401 时自动 refresh 或重新登录；当前 access 2h 内够用，但要补 refresh 流程。
7. **tabBar 图标缺失**：app.json tabBar 没配置 iconPath/selectedIconPath，tabBar 显示纯文字；微信开发者工具里可能警告。
8. **图片九宫格 2 列场景**：2-4 张用 two（2 列），3 张时最后一行一个会留空，可接受（朋友圈也是如此）。
9. **详情页底部输入栏 iPhone 安全区**：加了 `env(safe-area-inset-bottom)` 但未真机测。
10. **没装 TDesign**：按用户要求先走原生（避免「开发者工具构建 npm」门槛），风格是自写轻量样式；后续要统一视觉可再引 TDesign。

### 后续待做工作 / 方向
1. **空上下文独立测试**：启动后端 → curl mock 登录后，用微信开发者工具打开 `apps/user-miniprogram` 验证完整表白墙链路（登录→圈子→发帖→点赞→评论）
2. **后端 push**：`63639b5` 前次已推送成功确认？需再次确认 origin/main 状态
3. **提交**：`docs/开发记录/分支开发计划.md`（untracked）和本次前端一起 commit 到 feat/mp-confession → squash 到 main → push
4. **tabBar 图标**：准备 4 张 icon（81x81 png）加到 app.json tabBar.list
5. **refresh token 流程**：401 时调用 `/auth/refresh` 自动续期；失败再清 storage 跳登录
6. **图片并发上传 + 进度条**
7. **点赞节流**
8. **W2 启动 feat/merchant + feat/chat（按分支开发计划并行）**

---

## 2026-07-05 · Phase 1.2 表白墙后端（圈子 / 帖子 / 评论 / 点赞）

**发生时间**：2026-07-05 18:23 +0800
**状态**：已开发，typecheck 通过 + 端到端 smoke 通过；尚未提交 / 未运行时验证（真实凭证）
**commit**：未提交（待开 feat/confession 分支）

### 改动概要
表白墙后端 Phase 1.2：Circle 列表 / Post 发帖（内联内容安全）/ 发现信息流（游标分页）/ 帖子详情 / 点赞 toggle（去重）/ 评论 CRUD。新增 PostLike 模型 + Comment→Post/author 关系 + 迁移 + 默认圈子种子。端到端 smoke（mock 模式）全链路打通。

### 新增文件
- `apps/server/src/modules/confession/confession.module.ts`
- `apps/server/src/modules/confession/confession.controller.ts` — 8 个接口：`GET /circles`、`POST /circles/:id/posts`、`GET /circles/:id/posts`、`GET /posts/feed`、`GET /posts/:id`、`POST /posts/:id/like`、`POST /posts/:id/comments`、`GET /posts/:id/comments`
- `apps/server/src/modules/confession/confession.service.ts` — 游标分页（createdAt|id base64url）、点赞 toggle（transaction）、内联 checkText/checkImage
- `apps/server/src/modules/confession/dto/create-post.dto.ts` — content(1-2000) + images?(≤9)
- `apps/server/src/modules/confession/dto/create-comment.dto.ts` — content(1-500)
- `apps/server/src/modules/confession/dto/feed-query.dto.ts` — cursor?/limit?(1-50)
- `apps/server/src/modules/confession/dto/comments-query.dto.ts` — page?/pageSize?
- `apps/server/src/modules/confession/types.ts` — PostVo/CommentVo/FeedResult/PageResult/LikeResult
- `apps/server/prisma/seed.ts` — 5 个默认圈子（表白/失物招领/二手闲置/学习交流/日常闲聊），幂等

### 修改文件
- `apps/server/prisma/schema.prisma` — 新增 PostLike 模型（postId+userId 唯一）；Post 加 author/postLikes/comments 关系；Comment 加 post/author 关系；User 加 postLikes/posts/comments 反向关系
- `apps/server/src/app.module.ts` — 接入 ConfessionModule
- `apps/server/package.json` — 加 `prisma:seed` 脚本 + `prisma.seed` 配置

### 新增迁移
- `20260705102002_add_post_like` — post_likes 表 + comments 外键

### 完成的工作
- ✅ `GET /api/v1/circles`：圈子列表（5 个默认圈子已 seed）
- ✅ `POST /api/v1/circles/:id/posts`：发帖，内联 checkText(文本)+checkImage(每张图)，内容安全通过即 APPROVED 发布
- ✅ `GET /api/v1/posts/feed`：发现信息流，(createdAt DESC, id DESC) 游标分页，返回 `{list,nextCursor,hasMore}`
- ✅ `GET /api/v1/circles/:id/posts`：圈子内帖子列表（同游标分页）
- ✅ `GET /api/v1/posts/:id`：帖子详情（含作者/点赞数/评论数/是否已赞）
- ✅ `POST /api/v1/posts/:id/like`：点赞 toggle（PostLike 唯一约束去重，transaction 同步 likeCount），返回 `{liked,likeCount}`
- ✅ `POST /api/v1/posts/:id/comments`：评论，内联 checkText
- ✅ `GET /api/v1/posts/:id/comments`：评论分页（page/pageSize）
- ✅ 新增错误码 20003（帖子不存在），归入 2xxxx 表白墙段
- ✅ 端到端 smoke（mock 模式）：wx-login→circles→发帖→feed→点赞 toggle(1→0)→评论 全链路 200；无 token 返回 401
- typecheck（`tsc --noEmit`）通过

### 潜在隐患
1. **发帖即 APPROVED**：内容安全通过即发布，无人工审核前置；PENDING 仅留给 review 命中（当前 review 也未落 PENDING，仅告警）。Phase 1.6 管理端审核队列需补「待审」分支。
2. **review 命中未拦截**：msgSecCheck 返回 review（需人工复审）时当前只告警、仍 APPROVED 发布；应改为落 PENDING 入审核队列。
3. **点赞 race**：find-then-act 非原子，并发双击可能触发 PostLike 唯一约束 P2002 → 全局过滤器变 500。可改 upsert 或捕获 P2002 返回当前态。
4. **未运行时验证（真实凭证）**：smoke 仅 mock 模式；真实内容安全（checkText/checkImage 命中违规抛 90002）需配 WX 凭证验证。
5. **游标可伪造**：cursor 解码失败返回 null（视作首页），不报错；恶意构造只影响分页位置，无越权风险。
6. **无圈子分页**：circles 列表无分页（默认圈子少，MVP 可接受）。
7. **评论无二级回复**：仅平铺评论，无 nested reply（roadmap 未要求）。
8. **图片 URL 未校验归属**：发帖 images 传任意 URL，未校验是否本会话上传；可伪造他人图。可记录上传者或加签名。
9. **smoke 中文乱码**：Windows curl 以 GBK 发送中文 body，终端显示乱码；非服务端 bug（Node/PG 均按 UTF-8 处理，小程序客户端发 UTF-8 不受影响）。

### 后续待做工作 / 方向
1. **用户端小程序接入（Phase 1.2 前端）**：登录态存取、圈子页/发帖页/发现流/详情页 + 点赞评论交互。
2. **review → PENDING**：msgSecCheck/imgSecCheck 返回 review 时落 PENDING，入 Phase 1.6 审核队列。
3. **点赞并发**：改 upsert 或捕获 P2002 返回当前 liked 态，避免 500。
4. **运行时验证（真实凭证）**：配 WX 凭证 → 发违规文本/图片验证 90002 拦截。
5. **图片归属校验**：上传时记录 owner，发帖校验 images 归属当前用户。
6. **补单测**：游标分页边界、点赞 toggle 去重、圈子不存在 20001、帖子不存在 20003。
7. **提交**：开 feat/confession 分支提交本块 + 本改动记录。

---

## 2026-07-05 · Phase 1.1 收尾：COS 上传 + 内容安全工具

**发生时间**：2026-07-05 17:56 +0800
**状态**：已提交并推送 GitHub（main）；typecheck 通过
**commit**：`178b3b4`

### 改动概要
补齐 Phase 1.1 基础设施剩余两块：COS 图片上传接口 + 微信内容安全工具（msgSecCheck / imgSecCheck）封装。新增全局 WxAccessTokenService（平台级 access_token 缓存）+ moderation / upload 模块，接入 app.module。沿用 auth 的 mock 策略：开发环境缺凭证走 mock / 放行，生产环境缺凭证即报错。

### 新增文件
- `apps/server/src/common/wx/wx-access-token.service.ts` — 微信平台级 access_token 获取 + 内存缓存（提前 60s 过期）；缺凭证 dev 返回 mock、prod 抛 90003
- `apps/server/src/common/wx/wx.module.ts` — 全局 WxModule
- `apps/server/src/modules/moderation/moderation.service.ts` — `checkText`（msgSecCheck v2, scene=4）/ `checkImage`（imgSecCheck, media_url）；命中 risky 抛 90002，review 仅告警；接口故障 fail-open + 告警
- `apps/server/src/modules/moderation/moderation.module.ts` — 全局 ModerationModule（无 controller，供发帖/评论内联调用）
- `apps/server/src/modules/upload/upload.service.ts` — COS putObject 上传；缺凭证 dev 返回 mock URL、prod 抛 90003；key 按 `images/<date>/<uuid>` 组织
- `apps/server/src/modules/upload/upload.controller.ts` — `POST /uploads`（FileInterceptor，限 5MB，仅 jpg/png/webp/gif），返回 `{ url }`；需登录
- `apps/server/src/modules/upload/upload.module.ts`
- `apps/server/src/modules/upload/types.ts` — `MulterFile` 接口（规避 @types/multer 全局命名空间依赖）

### 修改文件
- `apps/server/src/app.module.ts` — 接入 WxModule / ModerationModule / UploadModule
- `apps/server/.env` — 增 `COS_SECRET_ID/KEY/BUCKET/REGION` 占位
- `.env.example` — 增 COS 段
- `apps/server/package.json` — 增 `cos-nodejs-sdk-v5` / `multer` / `@types/multer`

### 完成的工作
- ✅ `POST /api/v1/uploads`：登录态上传图片 → COS → 返回 URL（供发帖引用）
- ✅ `ModerationService.checkText/checkImage`：发帖/评论接口可内联调用，命中违规抛 90002（与 API 规范 §10 对齐）
- ✅ `WxAccessTokenService`：access_token 内存缓存复用，避免撞微信 gettoken 限额
- ✅ 沿用 mock 策略：开发环境缺 WX/COS 凭证可正常联调（mock URL / 跳过审核），生产环境缺凭证即 90003
- ✅ 新增错误码 90004（上传文件无效），归入 9xxxx 系统段
- typecheck（`tsc --noEmit`）通过

### 潜在隐患
1. **内容安全 fail-open**：msgSecCheck/imgSecCheck 接口故障或网络抖动时选择放行 + 告警，违规内容可能漏过；生产应改为可配置 fail-open/fail-closed 或失败转人工审核队列。
2. **mock 掩盖真实路径**：开发环境缺凭证时 upload 返回 mock URL、moderation 直接放行，真实 COS 上传与内容安全调用均需配齐凭证后验证（与 auth mock 同病）。
3. **未运行时验证**：仅 typecheck 通过；真实 COS 上传 + msgSecCheck/imgSecCheck 需配凭证后 curl 打通。
4. **access_token 单实例缓存**：内存缓存不跨进程；多实例部署时各实例独立 gettoken 可能撞限额。生产应抽到 Redis 共享缓存。
5. **msgSecCheck v2 需 openid**：当前 openid 可选传入，真实调用若不传可能被微信拒绝；发帖接口需把当前用户 openid 透传给 checkText。
6. **multer 超限错误未细化**：超过 5MB 时 multer 抛 LIMIT_FILE_SIZE，经全局过滤器变成 500「服务繁忙」，未返回友好 90004；可加 MulterException 适配。
7. **COS SDK 带废弃传递依赖**：cos-nodejs-sdk-v5 间接引入 request@2 / har-validator / uuid@3（已废弃），体积偏大；后续可评估换 STS 直传或更轻 SDK。
8. **无文件类型深度校验**：仅按 mimetype 判断，未校验文件魔数；可伪造 mimetype 上传非图片。

### 后续待做工作 / 方向
1. **运行时验证**：配 COS + WX 凭证 → curl `POST /uploads` 上传真图 → 在 confession 发帖接口调 `checkImage(url)` 验证拦截。
2. **接入 confession（Phase 1.2）**：发帖接口内联 `checkText(content, openid)` + 对每张图 `checkImage(url)`；评论接口内联 `checkText`。
3. **access_token 入 Redis**：共享缓存 + 分布式锁，支持多实例。
4. **fail 策略可配置**：moderation 增加 failMode（open/closed）配置项。
5. **multer 异常适配**：在 AllExceptionsFilter 或拦截器里把 MulterError(LIMIT_FILE_SIZE) 映射为 90004 + 400。
6. **文件魔数校验**：上传时读前几字节校验真实图片类型。
7. **补单测**：WxAccessTokenService 缓存/过期、ModerationService pass/risky/review/fail-open、UploadService mock/真实路径。
8. **提交**：开 feat/common 分支提交本块 + 本改动记录。

---

## 2026-07-05 · auth 模块代码审查修复

**发生时间**：2026-07-05 12:30 +0800
**状态**：已修复，typecheck 通过；尚未提交 / 未运行时验证
**commit**：未提交

### 改动概要
修复上一条「auth 模块（Phase 1.1）」代码审查发现的 7 处问题：1 个严重 bug（微信登录接口 URL 错误）+ 2 个安全/健壮性问题 + 4 个代码质量问题。

### 修改文件
- `apps/server/src/modules/auth/auth.service.ts` — ① code2session URL `jsauth2`→`jscode2session`（严重：生产环境微信登录 100% 失败）；② fetch 增加 10s 超时 + `res.ok` 检查 + 网络异常兜底；③ access/refresh token 签发时加 `type` 字段，refresh 校验仅放行 `type==='refresh'`；④ `getMe` 中 `401 as unknown as HttpStatus` 改为 `HttpStatus.UNAUTHORIZED`；⑤ upsert 补注释说明 role 设计意图
- `apps/server/src/modules/auth/auth.controller.ts` — `me` 防御性抛错状态码 400→401（`HttpStatus.UNAUTHORIZED`），补 import
- `apps/server/src/modules/auth/types.ts` — `JwtPayload` 增加 `type?: 'access' | 'refresh'` 字段
- `apps/server/src/modules/auth/jwt-auth.guard.ts` — 守卫校验仅放行 `type==='access'`，拒绝 refresh token 互换访问
- `apps/server/src/modules/auth/auth.module.ts` — 生产环境缺失 `JWT_SECRET` 时抛错，不再静默回退硬编码弱密钥

### 完成的工作
- ✅ 修复 code2session URL 拼写错误（`jsauth2`→`jscode2session`），生产环境微信登录可正常调用
- ✅ access/refresh token 增加 `type` 字段并双向校验，堵住互换漏洞（access 不能刷新、refresh 不能直接访问接口）
- ✅ fetch 增加超时（10s）、HTTP 状态码检查、网络异常 try/catch 兜底
- ✅ 统一所有鉴权相关异常的 HTTP 状态码为 401（`getMe`、`me` 防御性抛错）
- ✅ 生产环境 `JWT_SECRET` 缺失时启动即报错，避免弱密钥风险
- ✅ upsert 补注释明确「role 仅选 AppID，用户角色默认 USER，商家身份走 Merchant 表」
- typecheck（`tsc --noEmit`）通过

### 潜在隐患
1. **mock 仍掩盖真实路径**：开发环境未配凭证时走 mock，code2session 真实路径仍需配真实凭证后才能验证（本次 URL 修复即因此被 mock 掩盖，开发阶段完全暴露不出）。
2. **token 共用 secret 未分离**：本次仅加了 `type` 区分，access/refresh 仍共用 `JWT_SECRET`；生产应分离 `JWT_REFRESH_SECRET` + 服务端存 refresh 记录支持吊销（沿用上一条隐患，未完全解决）。
3. **未运行时验证**：仅 typecheck 通过，真实 code2session 调用需配 WX 凭证 + PostgreSQL 后验证。
4. **AbortSignal.timeout 兼容性**：依赖 Node 18+ 全局 `AbortSignal.timeout`；若运行时低于 Node 18 需改用 `AbortController` + `setTimeout`。

### 后续待做工作 / 方向
1. 运行时验证：配真实 WX 凭证后 curl 打通 wx-login/refresh/me，确认 code2session URL 修复生效。
2. 生产化 token：分离 `JWT_REFRESH_SECRET`；服务端存 refresh 记录支持吊销。
3. 补单测（P0）：覆盖 code2session 成功/失败、token type 校验（access 拒刷新 / refresh 拒访问）、JWT_SECRET 缺失启动失败。
4. 限流 + token 吊销（沿用上一条）。

---

## 2026-07-05 · auth 模块（Phase 1.1 鉴权地基）

**发生时间**：2026-07-05 12:02 +0800
**状态**：已开发，typecheck 通过；尚未提交 / 未运行时验证
**commit**：未提交（待开 `feat/auth` 分支）

### 改动概要
实现鉴权地基：微信登录 + JWT + 全局守卫 + 统一异常响应。

### 新增文件
- `apps/server/src/common/exceptions/biz.exception.ts` — 业务异常（携带业务错误码）
- `apps/server/src/modules/auth/types.ts` — `JwtPayload` / `AuthenticatedRequest` / `WxSessionResult`
- `apps/server/src/modules/auth/public.decorator.ts` — `@Public()` 白名单装饰器
- `apps/server/src/modules/auth/jwt-auth.guard.ts` — 全局 JWT 守卫
- `apps/server/src/modules/auth/dto/wx-login.dto.ts` — 登录 DTO（code/role/nickname?/avatarUrl?）
- `apps/server/src/modules/auth/dto/refresh-token.dto.ts` — 刷新 DTO
- `apps/server/src/modules/auth/auth.service.ts` — code2session + upsert + 签发/校验 token
- `apps/server/src/modules/auth/auth.controller.ts` — wx-login / refresh / me
- `apps/server/src/modules/auth/auth.module.ts` — 装配（全局 JwtModule）

### 修改文件
- `apps/server/src/common/filters/all-exceptions.filter.ts` — 兼容 class-validator 数组消息（join `'; '`）
- `apps/server/src/app.module.ts` — 接入 AuthModule，注册 `APP_GUARD = JwtAuthGuard`

### 完成的工作
- `POST /api/v1/auth/wx-login`：role 选 AppID → code2session → upsert User → 签发 access(2h)/refresh(7d)
- `POST /api/v1/auth/refresh`：校验 refresh → 重发 token
- `GET /api/v1/auth/me`：Bearer → 当前用户
- 全局 JwtAuthGuard：非 `@Public` 接口强制 Bearer，校验失败抛 `10001/10002`
- `BizException` + `AllExceptionsFilter` 统一 `{ code, message, traceId }`
- typecheck（`tsc --noEmit`）通过

### 潜在隐患
1. **token 安全**：access/refresh 共用 `JWT_SECRET`；refresh 泄露可续命 7 天。生产应：分离 refresh secret + 服务端存 refresh 记录支持吊销 + 缩短/轮换 refresh。
2. **mock code2session**：开发环境（`NODE_ENV≠production`）未配 WX 凭证时，**任意 code 都能登录**（返回 `mock_<code>`）。部署前务必：① 配齐 `WX_USER/MERCHANT_APPID/SECRET`；② 设 `NODE_ENV=production`。
3. **无限流**：wx-login/refresh 可被刷，未接限流（路线图 P1）。
4. **无 token 吊销**：用户被封禁后旧 token 仍有效至过期；需黑名单或短期 token + 强制刷新。
5. **用户资料可伪造**：nickname/avatarUrl 由前端传入，未校验真实性。需可信资料时改用 `wx.getUserProfile` 加密数据解密。
6. **无单测**：auth 属 P0 模块但尚未写测试（见开发规范 §10），需补 wxLogin/refresh/guard 单测。
7. **未运行时验证**：仅 typecheck 通过；需 PostgreSQL + 首次 `prisma migrate` 后才能跑通 wx-login。
8. **role 单值**：`User.role` 单字段，同一自然人既为学生又为商家时无法兼表；后续可能改多角色或经 Merchant 关联表达。
9. **code2session 用全局 fetch**：超时/重试未配置；网络抖动可能 5xx。建议加超时与重试。

### 后续待做工作 / 方向
1. **运行时验证**：起 PostgreSQL → 填 `DATABASE_URL` → `pnpm --filter @yitong/server prisma:migrate dev` → curl/Postman 打通 `wx-login / refresh / me`。
2. **接用户端小程序登录**：`wx.login` 拿 code → 调 `/auth/wx-login` → token 存 `globalData` + `wx.setStorageSync` → `/auth/me` 拉资料渲染「我的」。
3. **补单测（P0）**：Jest 写 wxLogin（mock code2session 成功/失败）、refresh（有效/过期/无效）、JwtAuthGuard（@Public 放行 / 无 token / 有效 / 过期）。
4. **生产化 token**：分离 `JWT_REFRESH_SECRET`；服务端存 refresh 记录支持吊销；去掉 mock（`NODE_ENV=production`）。
5. **限流**：`@nestjs/throttler` 对 `wx-login / refresh` 限流（如 5/min/IP）。
6. **token 吊销**：Redis 黑名单或缩短期 + 强制刷新。
7. **内容安全接入**：发帖前调 `msgSecCheck/imgSecCheck`（moderation 模块）。
8. **role 设计评估**：判断是否改多角色或经 Merchant 关联表达「学生+商家」双重身份。
9. **fetch 超时/重试**：或换 `@nestjs/axios` + interceptor。
10. **提交**：开 `feat/auth` 分支提交本模块 + 本改动记录。

---

## 2026-07-05 · 项目初始化骨架（commit 11ce0ae）

**发生时间**：2026-07-05 11:46:47 +0800
**状态**：已提交并推送 GitHub（main）
**commit**：`11ce0ae`

### 改动概要
pnpm monorepo 骨架 + NestJS/Prisma 后端 + 用户端原生小程序 + 共享包 + 依赖安装 + 首次提交。

### 新增文件（按类别）
- **根配置**：`.prettierrc` / `.editorconfig` / `eslint.config.mjs` / `tsconfig.base.json` / `package.json` / `pnpm-workspace.yaml` / `.gitignore` / `.env.example` / `README.md`
- **packages/shared-types**：`package.json` / `tsconfig.json` / `src/index.ts`（枚举/响应/分页类型）
- **packages/shared-utils**：`package.json` / `tsconfig.json` / `src/index.ts`（formatPrice/isBlank/sleep）
- **apps/server**：`package.json` / `tsconfig.json` / `tsconfig.build.json` / `nest-cli.json` / `src/main.ts` / `src/app.module.ts` / `src/prisma/{prisma.service,prisma.module}.ts` / `src/common/filters/all-exceptions.filter.ts` / `src/common/dto/api-response.ts` / `prisma/schema.prisma` / `.env`
- **apps/user-miniprogram**：`app.ts/json/wxss` / `sitemap.json` / `project.config.json` / `services/request.ts` / 4 个 tabBar 页面（confession/treehole/job/profile 各 index.ts + index.wxml）
- `docs/开发记录/初始化记录.md`
- 全部空目录加 `.gitkeep`

### 完成的工作
- pnpm monorepo 工作区（`apps/*` + `packages/*`）
- NestJS 可运行骨架 + Prisma schema（数据模型设计落地）+ Prisma Client 生成
- 原生小程序骨架（tabBar 四页 + `wx.request` 请求封装 + 统一错误提示）
- 共享包 build（CommonJS dist，供 server 消费）
- `pnpm install`（470 包）+ `pnpm.onlyBuiltDependencies`（prisma/nestjs）
- `git init` → 改名 `main` → 首次提交（75 文件）→ 推送 https://github.com/Alenchenxi/yitong.git
- server typecheck 通过、共享包 build 通过

### 潜在隐患
1. **admin-web / merchant-miniprogram 未 scaffold**：仅占位目录，待路线图 W2/W4。
2. **小程序 appid 占位**：`project.config.json` 用 `touristappid`，需在微信开发者工具替换为实际 AppID。
3. **.env 为占位值**：`apps/server/.env` 是本地占位（已 gitignore），生产凭证需单独配置；真实 WX/支付/TIM 凭证均未填。
4. **无 .gitattributes**：Windows 下提交出现 LF→CRLF 警告，跨端协作可能行尾抖动；建议加 `* text=auto eol=lf`。
5. **未配 tsconfig path 别名（@/）**：server 用相对路径（与开发规范示例的 `@/` 略有出入），因 NestJS 运行时不重写 paths；如需 `@/` 得配 tsconfig-paths。
6. **共享包变更需手动 rebuild**：`dev:server` 已串 build，但 watch 模式下共享包改动不会自动重建，可能引用旧 dist。
7. **依赖版本 ^ 范围**：lockfile 已锁定当前版本，但新机器 `pnpm install` 若锁文件丢失可能引入 minor 升级。
8. **Prisma 引擎**：首次 postinstall 被 pnpm 拦截，已手动 `prisma generate`；新机器装完需再跑一次 `prisma:generate`。
9. **未跑 migration**：schema 已就位但未建表，需用户配 DB 后执行 `prisma:migrate`。
10. **无 CI**：未配 GitHub Actions，typecheck/lint 暂靠本地。

### 后续待做工作 / 方向
1. **建库**：起 PostgreSQL → 填 `apps/server/.env` 的 `DATABASE_URL` → `pnpm --filter @yitong/server prisma:migrate dev` 建表。
2. **小程序接入**：微信开发者工具打开 `apps/user-miniprogram`，替换 `project.config.json` appid；按需 `npm i tdesign-miniprogram` + 工具内「构建 npm」。
3. **行尾规范**：加 `.gitattributes`（`* text=auto eol=lf`）消除 LF/CRLF 警告。
4. **路径别名**：配 `@/`（tsconfig paths + tsconfig-paths）贴合开发规范。
5. **补端 scaffold**：`apps/admin-web`（Vue3+Vite+Element Plus，W4）、`apps/merchant-miniprogram`（W2）。
6. **CI**：GitHub Actions（install + typecheck + lint + build）。
7. **共享包 watch**：引入 turbo 或 `pnpm -r --parallel run dev`，让共享包改动自动重建。
8. **依赖策略**：评估 `^` 是否改 `~` 或精确锁版本；定期 `pnpm update` 走单独 PR。
9. **填真实凭证**：WX AppID/Secret、微信支付商户号、TIM SDKAppID。
10. **部署**：腾讯云轻量服务器 + PM2 + Nginx（按技术栈文档）。

---

## 维护说明
- 每次代码改动后，在文件**顶部**追加一条，字段顺序：发生时间 → 状态 → commit → 改动概要 → 新增/修改文件 → 完成的工作 → 潜在隐患 → 后续待做工作/方向。
- 已消除的隐患标注 ✅ 已解决（日期）；已完成的「后续待做」标注 ✅。
- 提交后附 commit hash。
