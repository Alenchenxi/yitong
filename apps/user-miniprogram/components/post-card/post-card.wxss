.card {
  background: var(--yt-card);
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  border: 1rpx solid var(--yt-border);
}

.header {
  display: flex;
  align-items: center;
  margin-bottom: 16rpx;
}

.avatar {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: var(--yt-disabled-bg);
  margin-right: 16rpx;
}

.avatar-ph {
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #FFE99A, var(--yt-primary));
  color: var(--yt-text-main);
  font-size: 28rpx;
  font-weight: 600;
}

.meta { flex: 1; }

.nickname {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--yt-text-main);
}

.anon-badge {
  display: inline-block;
  margin-left: 8rpx;
  padding: 0 12rpx;
  font-size: 20rpx;
  font-weight: 500;
  color: var(--yt-text-main);
  background: #FFF7D1;
  border-radius: 999rpx;
  vertical-align: middle;
}

.time {
  font-size: 22rpx;
  color: var(--yt-text-sub);
  margin-top: 4rpx;
}

.content {
  font-size: 30rpx;
  line-height: 1.6;
  color: var(--yt-text-main);
  white-space: pre-wrap;
  word-break: break-word;
}

.images {
  display: grid;
  gap: 8rpx;
  margin-top: 16rpx;
}

.images-one { grid-template-columns: 1fr; }
.images-two { grid-template-columns: repeat(2, 1fr); }
.images-three { grid-template-columns: repeat(3, 1fr); }

.img {
  width: 100%;
  background: var(--yt-disabled-bg);
  border-radius: 8rpx;
}

.img-one { height: 360rpx; width: 540rpx; }
.img-two, .img-three { height: 200rpx; }

.tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 16rpx;
}

.tag {
  padding: 4rpx 18rpx;
  font-size: 22rpx;
  color: var(--yt-text-sub);
  background: var(--yt-disabled-bg);
  border-radius: 999rpx;
}

.video {
  width: 100%;
  height: 380rpx;
  margin-top: 16rpx;
  border-radius: 8rpx;
  background: #000;
}

.actions {
  display: flex;
  gap: 48rpx;
  margin-top: 20rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid var(--yt-border);
}

.action {
  display: flex;
  align-items: center;
  font-size: 26rpx;
  color: var(--yt-text-sub);
}

.icon {
  font-size: 32rpx;
  margin-right: 8rpx;
}

.icon.liked {
  color: var(--yt-primary);
}

.count { min-width: 32rpx; }
