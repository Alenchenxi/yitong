<view class="page">
  <!-- 顶部搜索 -->
  <view class="topbar">
    <view class="search-box" bindtap="goSearch">
      <text class="search-icon">🔍</text>
      <text class="search-ph">找人或搜索圈子</text>
    </view>
  </view>

  <!-- 公告栏 -->
  <view class="announce-bar" wx:if="{{announcements.length > 0}}">
    <swiper class="announce-swiper" autoplay circular interval="4000" vertical>
      <swiper-item wx:for="{{announcements}}" wx:key="id">
        <text class="announce-text">📢 {{item.title}}：{{item.content}}</text>
      </swiper-item>
    </swiper>
  </view>

  <!-- 主 tab：推荐 / 最新（CR-001:「关注」tab 移除，匿名帖无 follow 语义） -->
  <view class="main-tabs">
    <view class="main-tab {{activeMainTab === 'recommend' ? 'active' : ''}}" data-tab="recommend" bindtap="switchMainTab">推荐</view>
    <view class="main-tab {{activeMainTab === 'latest' ? 'active' : ''}}" data-tab="latest" bindtap="switchMainTab">最新</view>
  </view>

  <!-- 混合流 -->
  <view class="list" wx:if="{{items.length > 0}}">
    <block wx:for="{{items}}" wx:key="data.id">
      <!-- CR-001: 按 kind 分发渲染，匿名卡传 anonymous/disabled -->
      <view wx:if="{{item.kind === 'post'}}" data-id="{{item.data.id}}" data-kind="post" bindtap="goPostDetail">
        <post-card post="{{item.data}}" bind:like="onLike"></post-card>
      </view>
      <view wx:elif="{{item.kind === 'anon_post'}}" data-id="{{item.data.id}}" data-kind="anon_post" bindtap="goPostDetail">
        <post-card
          post="{{item.data}}"
          anonymous="{{true}}"
          disabled="{{!anonTokenReady}}"
          disabledReason="请前往树洞签发匿名身份后互动"
          bind:like="onLike"
        ></post-card>
      </view>
    </block>
    <view class="loading-tip" wx:if="{{loading}}">加载中...</view>
    <view class="loading-tip" wx:elif="{{!hasMore}}">没有更多了</view>
  </view>

  <view class="yt-empty" wx:elif="{{!loading}}">
    <view class="yt-empty-emoji">📭</view>
    <view class="yt-empty-text">还没有内容，去发第一条吧</view>
  </view>

  <!-- 悬浮发帖 -->
  <view class="fab" bindtap="goCreate">
    <text class="fab-plus">＋</text>
  </view>
</view>