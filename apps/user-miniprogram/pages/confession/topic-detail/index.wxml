<view class="page" wx:if="{{topic}}">
  <view class="header">
    <view class="name">#{{topic.name}}</view>
    <view class="desc" wx:if="{{topic.description}}">{{topic.description}}</view>
    <view class="count">{{total}} 篇内容</view>
  </view>
  <view class="list" wx:if="{{posts.length > 0}}">
    <post-card wx:for="{{posts}}" wx:key="id" post="{{item}}" bind:like="onLike"></post-card>
    <view class="loading-tip" wx:if="{{loading}}">加载中...</view>
    <view class="loading-tip" wx:elif="{{!hasMore}}">没有更多了</view>
  </view>
  <view class="yt-empty" wx:elif="{{!loading}}">
    <view class="yt-empty-emoji">📭</view>
    <view class="yt-empty-text">该话题暂无内容</view>
  </view>
</view>
